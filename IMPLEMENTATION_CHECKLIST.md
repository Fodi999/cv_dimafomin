# 🎯 ПЛАН ВНЕДРЕНИЯ МОНЕТИЗАЦИИ - Checklist

## 📋 Фаза 1: ПОДГОТОВКА (Неделя 1-2)

### 1.1 Наполнение контентом
- [ ] Создать папку `COURSE_CONTENT/` с описанием 10 курсов
- [ ] Каждый курс должен иметь:
  - [ ] Название и описание
  - [ ] 8-10 уроков с видео-ссылками
  - [ ] Квизы с вопросами
  - [ ] Финальный тест
  - [ ] Сертификат при завершении
  
**Пример курса:**
```
Курс "Основы Суши"
├─ Урок 1: История Суши (видео 10 мин)
├─ Урок 2: Инструменты (видео 5 мин)
├─ Урок 3: Рис и уксус (видео 8 мин)
├─ Урок 4: Нарезка рыбы (видео 15 мин)
├─ Урок 5: Техника скатывания (видео 12 мин)
├─ Урок 6: Ассортимент роллов (видео 15 мин)
├─ Урок 7: Презентация и плиттинг (видео 10 мин)
├─ Урок 8: Продвинутые техники (видео 20 мин)
├─ Квиз: 10 вопросов с ответами
└─ Финальный тест + Сертификат
```

### 1.2 Создание рецептов
- [ ] Загрузить 50+ рецептов с:
  - [ ] Фото блюда
  - [ ] Ингредиенты (количество, цена)
  - [ ] Пошаговые инструкции
  - [ ] Время приготовления
  - [ ] Уровень сложности
  - [ ] Питательная информация

### 1.3 Интеграция платежей
- [ ] Настроить Stripe аккаунт
- [ ] Добавить Stripe ключи в `.env.local`
- [ ] Создать endpoint для покупки CT
- [ ] Тестировать платежи в режиме разработки

---

## 💻 Фаза 2: РАЗРАБОТКА (Неделя 3-4)

### 2.1 Система начисления CT
```typescript
// lib/tokenSystem.ts - СОЗДАТЬ ФАЙЛ

export async function grantTokens(userId: string, amount: number, reason: string) {
  // POST /api/wallet/add-tokens
  // Причины: "course_completion", "lesson_complete", "post_created", "daily_bonus"
}

export async function deductTokens(userId: string, amount: number, reason: string) {
  // POST /api/wallet/deduct-tokens
}

export async function logTransaction(userId: string, type: 'earned' | 'spent', amount: number, reason: string) {
  // POST /api/wallet/transactions
}
```

### 2.2 API Endpoints (Backend)
- [ ] POST `/api/courses/:courseId/complete` → Начислить 100 CT
- [ ] POST `/api/lessons/:lessonId/complete` → Начислить 10-50 CT
- [ ] POST `/api/recipes/create` → Начислить 25 CT
- [ ] POST `/api/recipes/:recipeId/like` → Начислить +2 CT автору
- [ ] POST `/api/wallet/purchase-tokens` → Stripe платеж
- [ ] GET `/api/wallet/balance/:userId` → Получить баланс
- [ ] GET `/api/leaderboard` → Top 100 пользователей по CT

### 2.3 Frontend компоненты
- [ ] Обновить `/components/profile/TokenBalanceCard.tsx`
  - [ ] Добавить "Купить" кнопку
  - [ ] Показывать историю заработков
- [ ] Создать `/components/TokenPurchaseModal.tsx`
  - [ ] 5 пакетов CT
  - [ ] Stripe integration
  - [ ] Confirmation диалог
- [ ] Обновить `/app/academy/page.tsx`
  - [ ] Показывать "Завершить курс → +100 CT" сообщение

### 2.4 Реферальная система
```typescript
// lib/referral.ts - СОЗДАТЬ

export async function generateReferralLink(userId: string): Promise<string> {
  // https://yoursite.com/register?ref=USER_ID
}

export async function processReferral(referrerUserId: string, newUserId: string) {
  // Начислить 20% от заработков нового пользователя в течение 3 месяцев
}
```

---

## 🚀 Фаза 3: МАРКЕТПЛЕЙС (Неделя 5-6)

### 3.1 Создать маркетплейс компоненты
- [ ] `/market/products` → Все товары/курсы
- [ ] `/market/seller/:userId` → Профиль продавца
- [ ] `/market/seller/dashboard` → Панель для инструкторов
- [ ] `/market/sell/create-product` → Создать товар

### 3.2 Продавец функции
```typescript
// Панель продавца должна показывать:
- [ ] Доход за месяц
- [ ] Количество продаж
- [ ] Рейтинг (среднее)
- [ ] Список заказов
- [ ] Статистика просмотров
- [ ] Вывод заработков (минимум 1000 CT)
```

### 3.3 Покупатель функции
- [ ] Каталог товаров с фильтрами
- [ ] Поиск по названию и категориям
- [ ] Рейтинг и отзывы продавца
- [ ] Кнопка "Купить за CT"
- [ ] После покупки → доступ к контенту

---

## 📊 Фаза 4: АНАЛИТИКА И ОТЧЕТЫ (Неделя 7-8)

### 4.1 Админ-панель для владельца
- [ ] `/admin/analytics` → Общая статистика
  - [ ] Количество пользователей
  - [ ] Доход за день/неделю/месяц
  - [ ] Топ курсы
  - [ ] Топ инструкторы
  - [ ] Графики роста
  
### 4.2 Метрики для инструкторов
- [ ] Количество учащихся
- [ ] Рейтинг курса
- [ ] Процент завершения
- [ ] Отзывы
- [ ] Прогноз заработка

### 4.3 Метрики для пользователей
- [ ] Мой заработок за месяц
- [ ] Прогресс в курсах (%)
- [ ] Достигнутые уровни
- [ ] Сравнение с друзьями

---

## 🎁 Фаза 5: BONUSES И СТИМУЛЯЦИЯ (Неделя 9-10)

### 5.1 Ежедневный бонус
```typescript
// Реализовать систему:
- Первый вход в день → +10 CT
- Серия входов подряд → +5 CT за день (макс 50 CT за 10 дней)
```

### 5.2 Достижения
```typescript
- "Первый шаг" → Завершить 1 урок → 50 CT
- "Ученик" → Завершить 5 уроков → 100 CT
- "Мастер" → Завершить 50 уроков → 500 CT
- "Создатель" → Создать 10 рецептов → 300 CT
- "Популярный" → Получить 100 лайков → 200 CT
- "Учитель" → Создать курс → 1000 CT
```

### 5.3 События и вызовы
```
Еженедельный вызов:
"Создай 3 рецепта этой недели" → +100 CT бонус

Сезонное событие:
"Рождество 2024" → Дополнительные награды за курсы

День учителя:
Инструкторы получают +50% к заработкам
```

---

## 🔐 Фаза 6: БЕЗОПАСНОСТЬ И COMPLIANCE (Неделя 11-12)

### 6.1 Защита от мошенничества
- [ ] Проверка на подозрительные платежи
- [ ] Лимиты на вывод заработков (минимум 1000 CT)
- [ ] Верификация email перед выводом
- [ ] 2FA для больших операций

### 6.2 Terms of Service
- [ ] Написать Terms of Service
- [ ] Написать Privacy Policy
- [ ] Добавить GDPR compliant cookies
- [ ] Добавить Age Verification (18+)

### 6.3 Moderation
- [ ] Система флагирования контента
- [ ] Проверка рецептов перед публикацией
- [ ] Система рейтинга контента
- [ ] Удаление спама

---

## 📱 QUICK START CODE

### 1. Покупка CT (Stripe)
```typescript
// app/api/purchase-tokens/route.ts

import { stripe } from '@/lib/stripe';

export async function POST(request: Request) {
  const { amount, userId } = await request.json();
  
  const session = await stripe.checkout.sessions.create({
    payment_method_types: ['card'],
    line_items: [
      {
        price_data: {
          currency: 'usd',
          product_data: {
            name: `${amount} ChefTokens`,
          },
          unit_amount: Math.round(amount * 0.0999 * 100), // $0.0999 за токен
        },
        quantity: 1,
      },
    ],
    mode: 'payment',
    success_url: `${process.env.NEXT_PUBLIC_URL}/success?session_id={CHECKOUT_SESSION_ID}`,
    cancel_url: `${process.env.NEXT_PUBLIC_URL}/profile`,
    metadata: { userId, amount },
  });
  
  return Response.json({ url: session.url });
}
```

### 2. Ежедневный бонус
```typescript
// app/api/daily-bonus/route.ts

export async function POST(request: Request) {
  const { userId } = await request.json();
  
  const lastBonus = await db.bonus.findFirst({
    where: { userId },
    orderBy: { createdAt: 'desc' },
  });
  
  const now = new Date();
  const isToday = lastBonus?.createdAt.toDateString() === now.toDateString();
  
  if (!isToday) {
    // Начислить 10 CT + бонус за серию
    const streak = calculateStreak(userId);
    const bonus = 10 + (streak > 0 ? 5 : 0);
    
    await grantTokens(userId, bonus, 'daily_bonus');
    return Response.json({ success: true, bonus });
  }
  
  return Response.json({ success: false, message: 'Already claimed today' });
}
```

### 3. Завершение курса
```typescript
// app/api/courses/[courseId]/complete/route.ts

export async function POST(request: Request, params: any) {
  const { userId } = await request.json();
  const { courseId } = params;
  
  // Проверить, завершены ли все уроки
  const lessons = await db.lesson.findMany({ where: { courseId } });
  const completed = await db.completion.findMany({
    where: { userId, lessonId: { in: lessons.map(l => l.id) } }
  });
  
  if (completed.length === lessons.length) {
    // 100 CT награда
    await grantTokens(userId, 100, 'course_completion');
    
    // Выдать сертификат
    await issueCertificate(userId, courseId);
    
    return Response.json({ success: true, reward: 100 });
  }
  
  return Response.json({ success: false });
}
```

---

## 📈 МЕТРИКИ УСПЕХА

Отслеживайте каждую неделю:

```
Week 1: Контент готов?
Week 2: API endpoints готовы?
Week 3: Платежи работают?
Week 4: Маркетплейс готов?
Week 5: Реферальная система готова?
Week 6: Аналитика готова?

Финальный чек:
- [ ] Тестовые платежи прошли успешно
- [ ] Токены начисляются корректно
- [ ] Отчеты генерируются
- [ ] Нет ошибок в консоли
- [ ] Mobile версия работает
```

---

## 🎓 ОБУЧАЮЩИЕ РЕСУРСЫ

1. **Stripe документация**: https://stripe.com/docs
2. **Next.js API Routes**: https://nextjs.org/docs/api-routes
3. **Supabase для БД**: https://supabase.com/docs
4. **Vercel для деплоя**: https://vercel.com/docs

---

**Готово к внедрению? 🚀 Начни с Фазы 1! Успехов!**
