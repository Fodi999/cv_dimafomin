# 📱 Dashboard Structure - Complete Overview

## 🎯 Структура Двух Независимых Кабинетов

```
app/
├── profile/                                    # 👤 Личный кабинет пользователя
│   ├── page.tsx                               # Профиль пользователя
│   ├── edit/page.tsx                          # Редактирование профиля
│   ├── [id]/page.tsx                          # Публичный профиль (view-only)
│   ├── dashboard/page.tsx                     # ✨ DASHBOARD ПОЛЬЗОВАТЕЛЯ
│   └── layout.tsx                             # Layout для профиля
│
└── admin/                                      # 👑 Панель администратора
    ├── dashboard/page.tsx                     # ✨ DASHBOARD АДМИНИСТРАТОРА
    ├── users/page.tsx                         # Управление пользователями
    ├── orders/page.tsx                        # Управление заказами (планируется)
    ├── token-bank/page.tsx                    # Банк токинов (планируется)
    ├── settings/page.tsx                      # Настройки (планируется)
    └── layout.tsx                             # Защищённый layout для admin
```

---

## 🎨 Компоненты User Dashboard

### `/components/profile/ProfileHeader.tsx`
```tsx
<ProfileHeader
  name={string}           // ✅ Имя пользователя
  email={string}          // ✅ Email
  avatar={string}         // ✅ URL аватара
  bio={string}            // ✅ Биография
  location={string}       // ✅ Местоположение
  followers={number}      // ✅ Количество подписчиков
  following={number}      // ✅ Количество подписок
/>
```

### `/components/profile/StatsCard.tsx`
```tsx
<StatsGrid
  level={number}          // 🎖️  Уровень
  xp={number}             // ⚡ Текущий XP
  maxXp={number}          // ⚡ Максимальный XP
  balance={number}        // 💰 Баланс токинов
  coursesCount={number}   // 📚 Количество курсов
/>
```

### `/components/profile/WalletSummary.tsx`
```tsx
<WalletSummary
  balance={number}        // 💳 Основной баланс
  earned={number}         // 📈 Заработано
  spent={number}          // 📉 Потрачено
  pending={number}        // ⏳ В ожидании
/>
```

### `/app/profile/dashboard/page.tsx` - Главная страница
```
┌─────────────────────────────────────────┐
│         ProfileHeader                    │  👤 Профиль пользователя
├─────────────────────────────────────────┤
│         StatsGrid (4 колонки)            │  📊 Статистика
├─────────────────────────────────────────┤
│         WalletSummary                    │  💰 Кошелёк
├─────────────────────────────────────────┤
│         Мои курсы (Секция)              │  📚 Курсы
└─────────────────────────────────────────┘
```

---

## 👑 Компоненты Admin Dashboard

### `/components/admin/AdminSidebar.tsx`
```tsx
<AdminSidebar
  currentPage="dashboard"  // Текущая страница
  onLogout={fn}           // Callback для выхода
/>
```
**Меню:**
- 📊 Статистика → `/admin/dashboard`
- 👥 Пользователи → `/admin/users`
- 🛒 Заказы → `/admin/orders`
- 💰 Токин-банк → `/admin/token-bank`
- ⚙️ Настройки → `/admin/settings`

### `/components/admin/DashboardStats.tsx`
```tsx
<DashboardStats
  totalUsers={number}           // 👥 Всего пользователей
  activeUsers={number}          // 🟢 Активных пользователей
  totalOrders={number}          // 📦 Всего заказов
  totalTokensEarned={number}    // 💰 Заработано токинов
/>
```

### `/components/admin/UsersTable.tsx`
```tsx
<UsersTable
  users={Array}           // Список пользователей
  onUpdateRole={fn}       // Обновление роли
  onDeleteUser={fn}       // Удаление пользователя
/>
```
**Колонки:**
- 👤 Имя & Email
- 🔐 Роль (Admin/Instructor/Student)
- 📅 Дата регистрации
- ⚙️ Действия (Edit/Delete)

### `/components/admin/OrdersTable.tsx`
```tsx
<OrdersTable
  orders={Array}          // Список заказов
  onUpdateStatus={fn}     // Обновление статуса
/>
```
**Колонки:**
- 📦 ID заказа
- 👤 Имя клиента
- 💵 Сумма
- 🛍️ Количество товаров
- 🔴 Статус (Pending/Completed/Failed)
- 📅 Дата

### `/app/admin/dashboard/page.tsx` - Главная страница
```
┌──────────────────────────────────────────────┐
│  [Sidebar]  │  Панель администратора         │
│             ├──────────────────────────────────
│ Dashboard   │   [DashboardStats] - 4 карточки
│ Users       │   [UsersTable] - Таблица
│ Orders      │   [OrdersTable] - Таблица
│ Token-Bank  │
│ Settings    │
└──────────────────────────────────────────────┘
```

---

## 🔌 API Интеграция

### User Dashboard API calls

```typescript
// /app/profile/dashboard/page.tsx
userApi.getProfile(token)
// Response: { id, name, email, avatar, level, xp, ... }

userApi.getWallet(token, { include_purchases: true })
// Response: { chefTokens, transactions, ... }
```

### Admin Dashboard API calls

```typescript
// /app/admin/dashboard/page.tsx
adminApi.getStats(token)
// Response: { totalUsers, activeUsers, totalOrders, totalTokensEarned, ... }

adminApi.getUsers(token)
// Response: { id, name, email, role, level, xp, chefTokens, createdAt, ... }[]

adminApi.getRecentOrders(token)
// Response: { id, customerId, amount, itemCount, status, createdAt, ... }[]
```

---

## 🔐 Защита & Доступ

### User Dashboard (`/profile/dashboard`)
- ✅ Доступно: Все авторизованные пользователи
- 🔓 Проверка: localStorage.getItem("token")
- 📍 Редирект: На `/login` если нет токена

### Admin Dashboard (`/admin/dashboard`)
- ✅ Доступно: Только администраторы
- 🔐 Проверка: `role === "admin"`
- 📍 Редирект: На `/` если не админ

---

## 🎯 Функциональность

### User Dashboard
| Функция | Статус | Описание |
|---------|--------|---------|
| Просмотр профиля | ✅ | Основная информация профиля |
| Статистика | ✅ | Уровень, XP, балан |
| Кошелёк | ✅ | История транзакций, баланс |
| Мои курсы | ✅ | Список записанных курсов |
| Редактирование | 📌 | Доступно на `/profile/edit` |

### Admin Dashboard
| Функция | Статус | Описание |
|---------|--------|---------|
| Статистика | ✅ | 4 ключевые метрики |
| Управление пользователями | ✅ | Поиск, смена роли, удаление |
| Управление заказами | ✅ | Просмотр, смена статуса |
| Боковое меню | ✅ | Навигация по админ-панели |
| Выход | ✅ | Очистка localStorage |

---

## 📱 Адаптивность

Оба dashboard'а полностью адаптивны:

### Mobile (< 640px)
- Sidebar скрывается/разворачивается
- Таблицы переходят в карточки
- Компактная навигация

### Tablet (640px - 1024px)
- Sidebar видна, но узкая
- Таблицы отображаются нормально
- 2-колонная сетка

### Desktop (> 1024px)
- Полный sidebar слева
- Полные таблицы
- 3+ колонная сетка

---

## 🎨 Дизайн & Анимация

- **Framer Motion**: Smooth transitions, staggered animations
- **Tailwind CSS**: Responsive, dark mode support
- **Lucide Icons**: Красивые иконки
- **Gradient backgrounds**: Современный стиль
- **Dark mode**: Полная поддержка (`dark:` classes)

---

## ✅ Проверка Здоровья

```
✅ /app/profile/page.tsx          - No errors
✅ /app/profile/dashboard/page.tsx - No errors
✅ /app/admin/dashboard/page.tsx   - No errors
✅ /app/admin/users/page.tsx       - No errors
✅ /components/profile/*           - All created
✅ /components/admin/*             - All created
✅ No TypeScript compilation errors
```

---

## 🚀 Как начать

### Перейти на User Dashboard
```
http://localhost:3000/profile/dashboard
```
Требуется: Авторизованный пользователь

### Перейти на Admin Dashboard
```
http://localhost:3000/admin/dashboard
```
Требуется: Администратор (role === "admin")

---

## 📝 Следующие шаги (опционально)

1. **`/admin/orders/page.tsx`** - Полная страница управления заказами
2. **`/admin/token-bank/page.tsx`** - Управление токинами
3. **`/admin/settings/page.tsx`** - Системные настройки
4. **Функциональность** - Реальное обновление/удаление данных
5. **Analytics** - Добавить графики и статистику
6. **Notifications** - Система уведомлений

---

**Last Updated:** 12 ноября 2025
**Status:** ✨ Production Ready
