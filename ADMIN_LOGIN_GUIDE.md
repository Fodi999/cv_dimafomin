# 🔐 Вход в админ панель

## ✅ Требования

Для входа в админ панель вам нужны:
- **Email**: admin@example.com
- **Password**: admin_password_123
- **Role в БД**: должен быть `"admin"` (не `"user"` и не `"student"`)

## 🚀 Процесс входа

### 1. Откройте главную страницу
```
http://localhost:3000
```

### 2. Нажмите "Вход" (или перейдите на /login)

### 3. Введите учетные данные администратора
```
Email: admin@example.com
Password: admin_password_123
```

### 4. Нажмите кнопку "Вход"

### 5. Если вход успешен
- Вы должны видеть в консоли (F12 → Console):
  ```
  📋 User role from response: admin
  ✅ Login successful, response: {...role: "admin"...}
  ```

### 6. Перейдите в админ панель
```
http://localhost:3000/admin
```

## 🔍 Диагностика

### Если админ панель не открывается:

1. **Проверьте роль пользователя:**
   ```
   http://localhost:3000/debug-user
   ```
   Должно быть:
   - Role: **ADMIN** (красный текст)
   - Status: "Admin Access Granted"

2. **Если роль показывает "USER":**
   Это значит, что на backend пользователь admin имеет role = "user"
   
   **Решение:**
   - Обновите роль в БД backend на "admin"
   - Или используйте страницу setup:
     ```
     http://localhost:3000/admin-setup
     ```

3. **Если вы видите пустую страницу:**
   - Откройте консоль (F12)
   - Посмотрите на ошибки
   - Убедитесь, что backend запущен

## 📋 Откуда берется роль "admin"?

### Во время логина:

```
1. Frontend отправляет:
   POST /api/auth/login
   {"email": "admin@example.com", "password": "admin_password_123"}

2. Backend проверяет учетные данные в БД

3. Если пользователь найден и пароль верный:
   - Backend возвращает JWT токен с role = "admin"
   - Frontend получает: {"user": {"role": "admin"}, "token": "..."}

4. Frontend сохраняет:
   - localStorage.authToken = JWT token
   - Устанавливает user.role = "admin"

5. При открытии /admin:
   - Layout проверяет: user.role === "admin"
   - Если ДА → показывает админ панель
   - Если НЕТ → редиректит на главную
```

## 🔑 JWT токен

JWT токен содержит информацию о пользователе, включая роль:

```json
{
  "userId": "7ec8aba4-8195-4be1-a9a8-067c30aae306",
  "email": "admin@example.com",
  "role": "admin",  ← важно!
  "exp": 1762902620,
  "iat": 1762816220
}
```

Декодировать можно на https://jwt.io

## 🛠️ Если на backend нет админа

Если на backend нет пользователя с email "admin@example.com":

### Вариант 1: Регистрация админа
```bash
curl -X POST https://yeasty-madelaine-fodi999-671ccdf5.koyeb.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Admin User",
    "email": "admin@example.com",
    "password": "admin_password_123"
  }'
```

Затем в БД backend установите роль этого пользователя на "admin":
```sql
UPDATE users SET role = 'admin' WHERE email = 'admin@example.com';
```

### Вариант 2: Использование администраторского инструмента
```
http://localhost:3000/admin-setup
```

## 📚 Общая структура ролей

На сервере есть 3 роли:

| Роль | Доступ | Функции |
|------|--------|---------|
| `user` | Публичный | Чат, профиль, курсы |
| `instructor` | Модерация | Создание курсов, модерация |
| `admin` | Полный | Управление всем (админ панель) |

## ✨ Что вы сможете делать в админ панели

✅ Просматривать статистику (dashboard)
✅ Управлять пользователями (менять роли, удалять)
✅ Управлять заказами (менять статус)
✅ Менять настройки системы

## 🆘 Если что-то не работает

1. Откройте консоль браузера (F12 → Console)
2. Посмотрите логи (они начинаются с 📡, ✅, ❌)
3. Проверьте, что backend запущен
4. Убедитесь, что CORS настроен правильно

---

**Ключевой момент:** Frontend не создает и не проверяет роль - это делает backend при логине. Frontend просто использует `user.role` из ответа сервера!
