# 🔐 Auth Token Management & 401 Error Handling

## Проблема
Получаете ошибку **401 Unauthorized** при попытке доступа к защищённым endpoints (например, `/api/user/recipes/saved`)?

## Причины
1. **Токен истёк** - JWT токены имеют срок действия (обычно 24 часа)
2. **Токен невалидный** - повреждён или не соответствует формату
3. **Токен не передан** - отсутствует в localStorage

## Решение

### ✅ Автоматическая обработка (уже реализовано)

Мы добавили три уровня защиты:

#### 1. **TokenValidator** (глобальный)
- Проверяет токен при загрузке приложения
- Автоматически очищает невалидные токены
- Расположение: `components/auth/TokenValidator.tsx`

```tsx
// Подключён в app/layout.tsx
<TokenValidator />
```

#### 2. **authFetch** (для API запросов)
- Автоматически добавляет Authorization header
- Проверяет токен перед запросом
- Обрабатывает 401 и редиректит на логин

```tsx
// Используйте вместо обычного fetch
import { authFetch } from '@/lib/auth-interceptor';

const response = await authFetch('/api/user/recipes/saved');
```

#### 3. **isTokenValid** (проверка валидности)
- Проверяет формат JWT
- Проверяет срок действия (exp claim)

```tsx
import { isTokenValid } from '@/lib/auth-interceptor';

if (!isTokenValid()) {
  // Token expired or invalid
  router.push('/login');
}
```

### 📋 Пошаговая миграция существующего кода

#### Было (старый код):
```tsx
const token = localStorage.getItem('token');
const response = await fetch('/api/user/recipes/saved', {
  headers: {
    'Authorization': `Bearer ${token}`,
  },
});

if (response.status === 401) {
  localStorage.removeItem('token');
  router.push('/login');
}
```

#### Стало (новый код):
```tsx
import { authFetch } from '@/lib/auth-interceptor';

const response = await authFetch('/api/user/recipes/saved');
// 401 обрабатывается автоматически!
```

### 🔧 Что изменилось

#### `/app/recipes/saved/page.tsx`
- ✅ Использует `authFetch` вместо `fetch`
- ✅ Использует `isTokenValid()` для начальной проверки
- ✅ Показывает Toast уведомления при ошибках
- ✅ Автоматический редирект на `/login` при 401

#### `/lib/auth-interceptor.ts` (новый файл)
- `authFetch()` - обёртка над fetch с автообработкой 401
- `isTokenValid()` - проверка валидности токена
- `clearAuthAndRedirect()` - очистка данных и редирект

#### `/components/auth/TokenValidator.tsx` (новый компонент)
- Проверяет токен при загрузке приложения
- Автоматически очищает невалидные токены

### 🧪 Тестирование

#### 1. Тест с истёкшим токеном
```bash
# В консоли браузера
localStorage.setItem('token', 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE2MDAwMDAwMDB9.fake')
# Обновите страницу - токен должен быть очищен
```

#### 2. Тест 401 ответа
```bash
# Зайдите на /recipes/saved без токена
# Должен показать Toast и редирект на /login
```

### 📊 Логирование

Все действия логируются в консоль:

```
🔐 [TokenValidator] Checking token validity...
✅ [TokenValidator] Token is valid

🔐 [authFetch] Making authenticated request
❌ [authFetch] 401 Unauthorized - session expired
🔐 [AuthInterceptor] Clearing auth data and redirecting to login
```

### 🎯 Best Practices

1. **Всегда используйте `authFetch`** для защищённых endpoints
2. **Не храните чувствительные данные** в localStorage (только токен)
3. **Обновляйте токен** через refresh token механизм (TODO)
4. **Логируйте все auth события** для дебага

### 🔄 TODO: Refresh Token

Сейчас реализовано:
- ✅ Автоматическая проверка токена
- ✅ Автоматическая очистка при 401
- ✅ Редирект на login

Планируется:
- ⏳ Refresh token механизм
- ⏳ Автоматическое обновление токена за 5 минут до истечения
- ⏳ Silent authentication (без редиректа)

### 📚 Дополнительно

#### Структура JWT токена
```
Header.Payload.Signature

Payload содержит:
{
  "user_id": "123",
  "role": "student",
  "exp": 1735814400  // Unix timestamp
}
```

#### Проверка токена вручную
```tsx
const token = localStorage.getItem('token');
const payload = JSON.parse(atob(token.split('.')[1]));
console.log('Token expires:', new Date(payload.exp * 1000));
```

---

## 🐛 Если проблема сохраняется

1. Очистите localStorage полностью:
```javascript
localStorage.clear();
```

2. Перелогиньтесь заново через `/login`

3. Проверьте консоль браузера на ошибки

4. Проверьте Network tab - какой токен отправляется

---

Made with ❤️ by Dima Fomin
