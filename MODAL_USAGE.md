# 🚀 Модальное окно "Сайт в разработке"

## 📦 Установка и использование

### 1. Компонент создан в:
```
/components/DevelopmentModal.tsx
```

### 2. CSS анимации добавлены в:
```
/app/globals.css
```

### 3. Интеграция в главную страницу:
```
/app/page.tsx
```

---

## 🎨 Возможности компонента

### ✅ Основные функции:
- **Автоматическое появление** при загрузке страницы (через 500мс)
- **Затемнение фона** (overlay с 50% прозрачностью + blur эффект)
- **Плавная анимация появления** (scale + fade)
- **Закрытие модалки**:
  - По клику на кнопку "Понятно"
  - По клику на крестик (X)
  - По клику на overlay (фон)
- **Адаптивный дизайн** для мобильных и десктопов
- **Поддержка тёмной темы**
- **Accessibility** (ARIA атрибуты)

### 🎭 Доступные анимации:
В `globals.css` добавлены 4 варианта анимаций:
1. `animate-fade-in` - плавное появление overlay
2. `animate-modal-appear` - scale + fade (используется по умолчанию)
3. `animate-slide-up` - появление снизу вверх
4. `animate-bounce-in` - появление с отскоком

---

## 📝 Использование компонента

### Базовое использование:
```tsx
import DevelopmentModal from "@/components/DevelopmentModal";

export default function Page() {
  return (
    <>
      <DevelopmentModal />
      {/* Остальной контент страницы */}
    </>
  );
}
```

### С кастомным текстом:
```tsx
<DevelopmentModal 
  title="Технические работы"
  message="Сайт временно недоступен. Возвращайтесь позже!"
/>
```

### Props компонента:
```typescript
interface DevelopmentModalProps {
  title?: string;    // Заголовок (по умолчанию: "Сайт в разработке")
  message?: string;  // Сообщение (по умолчанию: "Скоро открытие!")
}
```

---

## 🎨 Смена анимации

### Вариант 1: Slide Up (снизу вверх)
В компоненте замените класс:
```tsx
className="... animate-modal-appear ..."
// на:
className="... animate-slide-up ..."
```

### Вариант 2: Bounce In (с отскоком)
```tsx
className="... animate-bounce-in ..."
```

---

## 🎯 Настройка появления модалки

### Изменить задержку появления:
В компоненте `DevelopmentModal.tsx` измените:
```tsx
const timer = setTimeout(() => {
  setIsOpen(true);
}, 500); // ← измените на нужное значение в мс
```

### Показывать модалку только один раз (с localStorage):
```tsx
useEffect(() => {
  const hasSeenModal = localStorage.getItem('hasSeenDevModal');
  
  if (!hasSeenModal) {
    const timer = setTimeout(() => {
      setIsOpen(true);
    }, 500);
    return () => clearTimeout(timer);
  }
}, []);

const handleClose = () => {
  setIsOpen(false);
  localStorage.setItem('hasSeenDevModal', 'true');
};
```

---

## 🎨 Кастомизация стилей

### Изменить цвета градиента:
```tsx
// В DevelopmentModal.tsx найдите:
<div className="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-sky-400 via-cyan-400 to-sky-500" />

// Измените цвета:
from-purple-400 via-pink-400 to-red-500
```

### Изменить цвет кнопки:
```tsx
// Найдите кнопку "Понятно":
className="... bg-gradient-to-r from-sky-500 to-cyan-500 ..."

// Измените на:
from-green-500 to-emerald-500  // Зелёный
from-purple-500 to-pink-500    // Фиолетовый
from-orange-500 to-red-500     // Красный
```

---

## 🚫 Временно отключить модалку

### Способ 1: Закомментировать в page.tsx:
```tsx
{/* <DevelopmentModal /> */}
```

### Способ 2: Добавить условие:
```tsx
{process.env.NODE_ENV === 'development' && <DevelopmentModal />}
```

### Способ 3: Через переменную окружения:
```tsx
{process.env.NEXT_PUBLIC_SHOW_DEV_MODAL === 'true' && <DevelopmentModal />}
```

---

## 📱 Адаптивность

Компонент полностью адаптивен:
- **Мобильные**: текст меньше, padding компактнее
- **Планшеты**: средние размеры
- **Десктоп**: полные размеры

Адаптивные классы используются для:
- `text-2xl sm:text-3xl` - размеры текста
- `px-8 py-3` - отступы
- `w-full max-w-md` - ширина модалки
- `p-4` - внешние отступы для мобильных

---

## 🎭 Альтернативные иконки

Замените иконку молотка на другую из `lucide-react`:

```tsx
import { Rocket, Construction, Sparkles, Zap } from "lucide-react";

// В JSX:
<Rocket className="w-8 h-8 text-white animate-bounce" />
```

---

## ⚡ Производительность

Компонент оптимизирован:
- ✅ Использует React Portal (рендер вне основного DOM)
- ✅ Условный рендеринг (`if (!isOpen) return null`)
- ✅ CSS анимации (hardware-accelerated)
- ✅ Минимальный размер компонента

---

## 🐛 Решение проблем

### Модалка не появляется:
1. Проверьте импорт компонента
2. Убедитесь, что `globals.css` подключен
3. Проверьте консоль на ошибки

### Анимация не работает:
1. Очистите кэш браузера
2. Перезапустите dev-сервер
3. Проверьте наличие анимаций в `globals.css`

### z-index конфликты:
Модалка использует `z-[9999]`. Если нужно изменить:
```tsx
className="fixed inset-0 z-[10000] ..." // ← увеличьте
```

---

## 📚 Дополнительные возможности

### Добавить таймер автозакрытия:
```tsx
useEffect(() => {
  if (isOpen) {
    const timer = setTimeout(() => {
      setIsOpen(false);
    }, 5000); // Закроется через 5 секунд
    
    return () => clearTimeout(timer);
  }
}, [isOpen]);
```

### Добавить кнопку "Не показывать снова":
```tsx
<button
  onClick={() => {
    localStorage.setItem('hideDevModal', 'true');
    handleClose();
  }}
  className="text-xs text-gray-500 underline"
>
  Не показывать снова
</button>
```

---

## ✨ Готово!

Модальное окно полностью готово к использованию и настроено для вашего проекта! 🎉

Если нужна помощь с кастомизацией - обращайтесь! 🚀
