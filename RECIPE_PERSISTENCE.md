# 📦 Система збереження рецептів

## Як це працює

Тепер всі рецепти, які ви створюєте, автоматично зберігаються в **localStorage** браузера. Це означає, що вони залишаються доступні навіть після:

- ✅ Перезагрузки сторінки (F5)
- ✅ Закриття та відкриття браузера
- ✅ Перезавантаження комп'ютера
- ❌ Очищення кеша браузера (видалить всі рецепти)

## Де зберігаються дані

**localStorage key:** `recipes`

Ви можете переглянути дані в DevTools:
```
Chrome: F12 → Application → Local Storage → https://your-domain → "recipes"
```

## Операції, які зберігаються

### 1. **Створення рецепту**
```
handleCreateRecipe() → localStorage.setItem("recipes", JSON.stringify(updatedRecipes))
```
- ✅ Зберігається 100% рецепту (фото, відео, ингредієнти, кроки)
- ✅ Генерується унікальний ID на основі timestamp
- ✅ Зберігаються дати створення/обновлення

### 2. **Редагування рецепту**
```
handleEditRecipe() → localStorage.setItem("recipes", JSON.stringify(updatedRecipes))
```
- ✅ Оновлює іcнуючий рецепт
- ✅ Оновлює поле `updatedAt`

### 3. **Видалення рецепту**
```
handleDeleteRecipe() → localStorage.setItem("recipes", JSON.stringify(updatedRecipes))
```
- ✅ Видаляє рецепт з масиву
- ✅ Оновлює localStorage

## Структура даних

Кожен рецепт має таку структуру:

```typescript
{
  id: string;                                           // Унікальний ID (timestamp)
  name: string;                                         // Назва рецепту
  description: string;                                  // Повний опис
  image: string;                                        // Першої фото (або емодзі)
  cuisine: string;                                      // Тип кухні
  difficulty: "easy" | "medium" | "hard";              // Складність
  prepTime: number;                                     // Час підготовки (хвилин)
  cookTime: number;                                     // Час варіння (хвилин)
  servings: number;                                     // Кількість порцій
  calories: number;                                     // Калорій на порцію
  price: number;                                        // Ціна в токенах
  rating: number;                                       // Рейтинг (0-5)
  reviews: number;                                      // Кількість відгуків
  status: "draft" | "published" | "archived";          // Статус
  author: string;                                       // Автор
  tags: string[];                                       // Теги для пошуку
  views: number;                                        // Кількість переглядів
  purchases: number;                                    // Кількість покупок
  revenue: number;                                      // Заробіток в токенах
  createdAt: Date;                                      // Дата створення
  updatedAt: Date;                                      // Дата останнього обновлення
  ingredients?: Array<{                                 // Ингредієнти
    name: string;
    quantity: number;
    unit: string;
  }>;
  instructions?: string[];                              // Пошагові інструкції
  youtubeUrl?: string;                                  // URL YouTube відео
  images?: string[];                                    // Масив всіх фото (base64 або емодзі)
}
```

## Завантаження даних при старті

При першій загрузці сторінки:

```typescript
useEffect(() => {
  const savedRecipes = localStorage.getItem("recipes");
  if (savedRecipes) {
    // Завантажуємо збережені рецепти
    const parsedRecipes = JSON.parse(savedRecipes);
    setRecipes(parsedRecipes);
  } else {
    // Якщо нічого не збережено, використовуємо mock дані
    setRecipes(mockRecipes);
  }
}, []);
```

## Приклад

1. **Створюєте новий рецепт "Aglio e Olio"**
   ```
   → handleCreateRecipe() викликається
   → Рецепт додається до масиву
   → localStorage.setItem("recipes", ...) зберігає все
   ```

2. **Перезавантажуєте сторінку (F5)**
   ```
   → Сторінка загружується
   → useEffect() запускається
   → Читає з localStorage
   → Рецепт "Aglio e Olio" все ще там! ✅
   ```

3. **Закриваєте браузер**
   ```
   → Закриваєте Chrome, Firefox, Safari
   → Дані в localStorage залишаються
   → Завтра відкриваєте браузер
   → Рецепт все ще там! ✅
   ```

## ⚠️ Обмеження localStorage

- **Обмеження розміру:** ~5-10MB (залежить від браузера)
- **Синхронна операція:** Може тримати сторінку при великих обсягах
- **Повна очистка:** Видалення кеша браузера видалить все

## Майбутні покращення

Для виробничого використання рекомендується:

1. **Backend API** - зберігати на сервері
2. **IndexedDB** - більші обсяги даних
3. **Database (PostgreSQL, MongoDB)** - надійне збереження
4. **Auto-sync** - синхронізація з сервером

## Консоль для отримання даних

У браузерній консолі можна переглянути збережені рецепти:

```javascript
// Отримати всі рецепти
JSON.parse(localStorage.getItem('recipes'))

// Отримати кількість рецептів
JSON.parse(localStorage.getItem('recipes')).length

// Видалити всі рецепти
localStorage.removeItem('recipes')
```

---

**Статус:** ✅ Успішно реалізовано
**Дата останнього обновлення:** 17 листопада 2025
