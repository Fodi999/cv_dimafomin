# ⚠️ Необходимо перезапустить dev server

## Причина
Были внесены изменения в API route `/app/api/admin/users/[id]/route.ts`, и Turbopack может не подхватить изменения автоматически.

## Что сделать

### 1. Остановить текущий dev server
В терминале, где запущен `npm run dev`, нажмите `Ctrl+C`

### 2. Очистить кэш (опционально)
```bash
rm -rf .next
```

### 3. Запустить заново
```bash
npm run dev
```

## Ожидаемый результат

После перезапуска в логах должно появиться:
```
✅ [GET User Details] Admin: fodi85@gmail.ru, Target user: 407582be-59d5-4d21-873b-1a72d31b0d42
📤 [GET User Details] Trying: GET https://yeasty-madelaine-fodi999-671ccdf5.koyeb.app/api/admin/users/407582be-59d5-4d21-873b-1a72d31b0d42
📥 [GET User Details] Backend status: 200
✅ [GET User Details] Success
```

Если получите 405, попробует автоматически `/api/users/:id` вместо `/api/admin/users/:id`.

---

**Дата:** 2026-01-26  
**Статус:** Ожидает перезапуска
