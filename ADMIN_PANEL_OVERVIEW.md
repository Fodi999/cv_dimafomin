# 🛡️ Admin Panel Overview - Modern Food Academy

## Краткое резюме

Администраторская панель управляет всей платформой через **4 основных раздела**:

| Раздел | Путь | Функция | Статус |
|--------|------|---------|--------|
| **Dashboard** | `/admin/dashboard` | Статистика, пользователи, заказы | ✅ Готов |
| **Token Bank** | `/admin/token-bank` | Выделение/отзыв токинов | ✅ Готов |
| **Users** | `/admin/users` | Управление пользователями | ✅ Готов |
| **Settings** | `/admin/settings` | Настройки панели | ✅ Готов |

---

## 1️⃣ Dashboard (`/admin/dashboard`)

### Назначение
Главная страница администратора - обзор статистики и отчеты по системе

### Компоненты
```
Dashboard
├─ DashboardStats (4 карточки)
│  ├─ Всего пользователей
│  ├─ Активных пользователей  
│  ├─ Всего заказов
│  └─ Всего токинов в системе
├─ UsersTable (список пользователей)
│  ├─ Имя, Email, Роль
│  ├─ Уровень (XP)
│  ├─ ChefTokens баланс
│  └─ Дата создания
└─ OrdersTable (список заказов)
   ├─ ID заказа, пользователь
   ├─ Сумма, статус
   └─ Дата создания
```

### Данные (Mock + Real Fallback)
```typescript
mockStats = {
  totalUsers: 1250,
  activeUsers: 348,
  totalOrders: 5420,
  totalRevenue: 125000,
  pendingOrders: 12,
  averageOrderValue: 23.07,
  totalTokensEarned: 450000
}
```

### API Calls
```
GET /api/admin/dashboard      // Основная статистика
GET /api/admin/users           // Список пользователей
GET /api/admin/orders          // Список заказов
```

### Особенности
- ✅ Role-based access (проверяет `localStorage.role === 'admin'`)
- ✅ Fallback на mock-данные если API недоступен
- ✅ Realtime статистика (обновляется при загрузке)
- ✅ Responsive дизайн (mobile-first)

---

## 2️⃣ Token Bank (`/admin/token-bank`)

### Назначение
Управление ChefTokens - выделение, отзыв, статистика

### Функции

#### a) Просмотр всех токин-банков пользователей
```
Таблица с колонками:
- Пользователь (имя/ID)
- Баланс (текущие токины)
- Заработано (история)
- Потрачено (история)
- Последняя транзакция
- Действия (выделить/отозвать)
```

#### b) Выделение токинов
```
Modal: "Выделить токины"
├─ Выбрать пользователя
├─ Ввести количество
├─ Выбрать причину (bonus, reward, refund, etc.)
└─ Кнопка "Выделить"

Действие:
- Добавляет токины на баланс пользователя
- Записывает транзакцию в историю
- Обновляет статистику системы
```

#### c) Отзыв токинов
```
Действие:
- Кнопка "Отозвать" удаляет 10% баланса
- Требует подтверждения
- Минимум баланс = 0
- Записывает в историю с reason: "admin_revoke"
```

### API Endpoints
```typescript
GET    /api/admin/token-bank              // Все токин-банки
GET    /api/admin/token-bank/stats        // Статистика токинов
POST   /api/admin/token-bank/allocate     // Выделить токины
POST   /api/admin/token-bank/revoke       // Отозвать токины
PUT    /api/admin/token-bank/balance      // Установить баланс
```

### Request/Response Примеры

**Выделить токины:**
```json
POST /api/admin/token-bank/allocate
{
  "user_id": "user-001",
  "amount": 500,
  "reason": "bonus"  // bonus, reward, refund, admin_allocation
}

Response: {
  "message": "✅ Токины выделены"
}
```

**Отозвать токины:**
```json
POST /api/admin/token-bank/revoke
{
  "user_id": "user-001",
  "amount": 125,
  "reason": "admin_revoke"
}

Response: {
  "message": "✅ Токины отозваны"
}
```

**Установить баланс напрямую:**
```json
PUT /api/admin/token-bank/balance
{
  "user_id": "user-001",
  "balance": 1000
}

Response: {
  "message": "✅ Баланс установлен"
}
```

### Mock Данные (5 пользователей)
```
Александр Петров    - 1250 токинов (заработано: 5000, потрачено: 3750)
Мария Иванова       - 3500 токинов (заработано: 8000, потрачено: 4500)
Сергей Смирнов      - 750 токинов  (заработано: 2000, потрачено: 1250)
Елена Кузнецова     - 2100 токинов (заработано: 6000, потрачено: 3900)
Дмитрий Морозов     - 4200 токинов (заработано: 9500, потрачено: 5300)
```

---

## 3️⃣ Users (`/admin/users`)

### Назначение
Управление учетными записями пользователей - редактирование, удаление, смена ролей

### Функции

#### a) Список пользователей
```
Таблица с колонками:
- Имя / Email (с поиском)
- Роль (student/instructor/admin)
- Уровень (XP)
- ChefTokens баланс
- Дата создания
- Действия
```

#### b) Редактирование пользователя
```
Можно изменить:
- Имя
- Email
- Роль (student → instructor → admin)
- Статус активности
```

#### c) Удаление пользователя
```
- Требует подтверждения
- Удаляет из системы полностью
- Очищает все данные (заказы, токины, курсы)
```

### API Endpoints
```typescript
GET    /api/admin/users                   // Список всех
GET    /api/admin/users/{userId}          // Один пользователь
PUT    /api/admin/users/{userId}          // Обновить данные
PATCH  /api/admin/users/update-role       // Смена роли
DELETE /api/admin/users/{userId}          // Удалить пользователя
```

### Request/Response Примеры

**Обновить пользователя:**
```json
PUT /api/admin/users/user-001
{
  "name": "Новое имя",
  "email": "newemail@example.com"
}

Response: {
  "data": { ...updated user data }
}
```

**Смена роли:**
```json
PATCH /api/admin/users/update-role
{
  "user_id": "user-001",
  "role": "instructor"
}

Response: {
  "message": "✅ Роль изменена"
}
```

**Удалить пользователя:**
```json
DELETE /api/admin/users/user-001

Response: {
  "message": "✅ Пользователь удален"
}
```

### Mock Данные (3 примера)
```
John Doe       - john@example.com   - student  - Level 5  - 1250 tokens - Создан 30 дней назад
Jane Smith     - jane@example.com   - instructor - Level 12 - 3450 tokens - Создан 60 дней назад
Mike Johnson   - mike@example.com   - student  - Level 3  - 450 tokens  - Создан 7 дней назад
```

---

## 4️⃣ Settings (`/admin/settings`)

### Назначение
Настройки администраторской панели и системных параметров

### Секции

#### 📢 Уведомления
- [ ] Email уведомления (получать на email)
- [ ] Push уведомления (браузерные уведомления)

#### 🎨 Интерфейс
- [ ] Тёмный режим (темная тема оформления)

#### 💾 Данные
- [ ] Автоматическое резервное копирование (ежедневно)

#### 🔒 Безопасность
- Смена пароля
- Двухфакторная аутентификация
- Сессии и вход
- Выход из аккаунта

### Сохранение
- Настройки сохраняются в `localStorage` (ключ: `adminSettings`)
- Может быть расширено на backend
- Показывает "✓ Настройки сохранены успешно" на 3 сек

---

## 📊 Admin API Structure (`src/lib/admin-api.ts`)

Централизованный клиент для всех админ-операций:

```typescript
export const adminApi = {
  // Profile
  getProfile()
  
  // Dashboard
  getStats()
  
  // Users
  getUsers()
  updateUser(userId, data)
  updateUserRole(userId, role)
  deleteUser(userId)
  
  // Orders
  getOrders()
  getRecentOrders()
  updateOrderStatus(orderId, status)
  
  // Token Bank
  getTokenBanks()
  getTokenStats()
  getUserTokenBank(userId)
  allocateTokens(userId, amount, reason)
  revokeTokens(userId, amount, reason)
  setTokenBalance(userId, balance)
}
```

### Особенности adminApi
- ✅ Автоматическое добавление Bearer токена
- ✅ Обработка 401/403 (редирект на /login при неавторизованности)
- ✅ Логирование всех запросов
- ✅ Обработка JSON ошибок
- ✅ Кэширование отключено (cache: 'no-store')

---

## 🔐 Система доступа (Role-Based Access Control)

### Проверка прав (Frontend)
```typescript
// В app/admin/page.tsx
useEffect(() => {
  const role = localStorage.getItem('role');
  if (role !== 'admin') {
    router.push('/');  // Редирект если не админ
  }
}, []);
```

### Уровни ролей
```
- student      Базовый пользователь (доступ к курсам, чату, маркету)
- instructor   Преподаватель (может создавать курсы)
- admin        Администратор (полный доступ к панели)
```

### Проверка на Backend
Должна быть реализована на backend'е:
```
1. Получение токена из Authorization header
2. Проверка JWT (если используется)
3. Проверка роли в базе данных
4. Возврат 403 Forbidden если не админ
```

---

## 🚀 Интеграция с остальной системой

### Взаимосвязь с другими разделами

```
Admin Panel (Управление системой)
├─ → User Profiles (редактирование профилей пользователей)
├─ → Wallet (выделение/отзыв токинов)
├─ → Orders (отслеживание покупок)
├─ → Courses (анализ прогресса)
└─ → Market (статистика продаж рецептов)
```

### Эффекты админ-операций

**Выделение токинов:**
- Баланс пользователя увеличивается
- История транзакций обновляется
- WalletCard показывает новый баланс

**Удаление пользователя:**
- Удаляются все заказы
- Освобождаются токины (или переводятся в "сирот")
- Очищается история профиля

**Смена роли:**
- Пользователь получает новые права
- Может начать создавать курсы (если instructor)
- Может получить доступ к админ функциям (если admin)

---

## 📝 Состояние реализации

### ✅ Готово (Frontend)
- [x] Dashboard дизайн и компоненты
- [x] Token Bank интерфейс
- [x] Users таблица и управление
- [x] Settings страница
- [x] Mock данные для тестирования
- [x] API клиент (adminApi)
- [x] Role-based access check
- [x] Error обработка и fallback'и

### ⏳ Требует Backend
- [ ] Реальные эндпоинты API (`/api/admin/*`)
- [ ] JWT токены и аутентификация
- [ ] Проверка прав на backend'е
- [ ] Логирование всех админ-операций
- [ ] Аудит: кто и когда сделал какое действие
- [ ] Резервное копирование при удалении пользователей

### 📊 Рекомендуемый порядок Backend реализации
1. **Phase 1**: Authenticate & Auth (JWT, role check)
2. **Phase 2**: Admin Dashboard Stats API
3. **Phase 3**: Users management API
4. **Phase 4**: Orders management API
5. **Phase 5**: Token Bank API (allocate/revoke)
6. **Phase 6**: Settings & Audit logging

---

## 🔗 Файловая структура

```
app/
├─ admin/
│  ├─ page.tsx              (redirect → /admin/dashboard)
│  ├─ layout.tsx            (navbar, auth check)
│  ├─ dashboard/
│  │  └─ page.tsx          (stats, users, orders)
│  ├─ users/
│  │  └─ page.tsx          (user management)
│  ├─ token-bank/
│  │  └─ page.tsx          (token allocation/revocation)
│  └─ settings/
│     └─ page.tsx          (admin settings)
│
src/lib/
├─ admin-api.ts            (API client)
└─ types.ts                (TypeScript interfaces)
```

---

## 🎯 Краткая инструкция для админа

### Типичные сценарии

**Сценарий 1: Пользователь заполнил анкету на стипендию**
1. Идти в `/admin/users`
2. Найти пользователя по имени
3. Нажать "Edit"
4. Установить роль "instructor"
5. Сохранить

**Сценарий 2: Отправить бонус-токины новому пользователю**
1. Идти в `/admin/token-bank`
2. Найти пользователя
3. Нажать "+" (выделить)
4. Ввести 100 токинов
5. Выбрать reason: "bonus"
6. Подтвердить

**Сценарий 3: Отменить неправомерную покупку в маркете**
1. Идти в `/admin/dashboard` → Orders
2. Найти заказ
3. Изменить статус на "cancelled"
4. Идти в `/admin/token-bank`
5. Отозвать токины у пользователя (вернуть них)

**Сценарий 4: Удалить спам-аккаунт**
1. Идти в `/admin/users`
2. Найти пользователя
3. Нажать "Delete"
4. Подтвердить
5. Аккаунт полностью удален

---

## 📞 Контакт для вопросов

- **Разработка Panel**: FE полностью готов, нужен Backend
- **Вопросы по API**: Смотри `/api/admin/*` endpoints
- **Вопросы по данным**: Mock в каждой компоненте

---

**Last Updated**: 2025-01-15  
**Status**: ✅ UI Complete, ⏳ Backend Pending
