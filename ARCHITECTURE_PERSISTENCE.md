# 🔄 Архитектура системы збереження рецептів

## Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────────┐
│                    REACT КОМПОНЕНТ                              │
│                   RecipesPage.tsx                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  STATE:                                                          │
│  - recipes: Recipe[]          ← Массив всех рецептов            │
│  - filteredRecipes: Recipe[]  ← Отфильтрованные рецепты         │
│                                                                  │
│  HOOKS:                                                          │
│  - useEffect(() => {          ← Загружает при старте            │
│      const saved = localStorage.getItem("recipes")              │
│      setRecipes(saved ? parsed : mockRecipes)                   │
│    }, [])                                                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
           ↓                          ↓                         ↓
    ┌──────────────┐        ┌─────────────────┐      ┌──────────────┐
    │   Создание   │        │  Редактирование │      │  Удаление    │
    └──────────────┘        └─────────────────┘      └──────────────┘
           ↓                          ↓                         ↓
┌────────────────────┐    ┌──────────────────────┐  ┌──────────────────┐
│handleCreateRecipe()│    │ handleEditRecipe()   │  │handleDeleteRecipe│
├────────────────────┤    ├──────────────────────┤  ├──────────────────┤
│1. Создает объект   │    │1. Обновляет рецепт  │  │1. Удаляет рецепт │
│   Recipe с полями  │    │   по ID              │  │   из массива     │
│2. Добавляет в      │    │2. Обновляет поле    │  │2. Обновляет      │
│   recipes array    │    │   updatedAt          │  │   localStorage   │
│3. Вызывает        │    │3. Сохраняет в       │  │3. Обновляет UI   │
│   setRecipes()    │    │   localStorage       │  │                  │
│4. Сохраняет в     │    │4. Обновляет UI      │  │                  │
│   localStorage     │    │                      │  │                  │
└────────────────────┘    └──────────────────────┘  └──────────────────┘
           ↓                          ↓                         ↓
        ┌──────────────────────────────────────────────────────┐
        │    localStorage.setItem("recipes", JSON.stringify())   │
        │                                                       │
        │  💾 Браузер сохраняет JSON в localStorage            │
        └──────────────────────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────────────────────┐
        │    localStorage (браузер, персистентное хранилище)    │
        │                                                       │
        │  Key: "recipes"                                      │
        │  Value: [                                            │
        │    { id, name, description, cuisine, ..., images }, │
        │    { id, name, description, cuisine, ..., images }, │
        │    ...                                               │
        │  ]                                                   │
        └──────────────────────────────────────────────────────┘
```

## Жизненный цикл приложения

```
┌─────────────────┐
│  БРАУЗЕР ОТКУПУ  │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────────┐
│  React монтирует компонент      │
│  RecipesPage.tsx загружается    │
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│  useEffect запускается          │
│  (пустой dependency array)      │
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│  localStorage.getItem("recipes")│
└────┬────────────────┬───────────┘
     │                │
     ↓ ЕСТЬ ДАННЫЕ    ↓ НЕТ ДАННЫХ
  JSON.parse()    mockRecipes
     │                │
     └────────┬───────┘
              ↓
      setRecipes(data)
      setFilteredRecipes(data)
              │
              ↓
    ┌──────────────────┐
    │  UI показывает   │
    │  рецепты на      │
    │  странице        │
    └────────┬─────────┘
             │
    ┌────────┴─────────────────────┐
    │ Пользователь может:           │
    │ - Создать новый рецепт        │
    │ - Отредактировать существующ  │
    │ - Удалить рецепт              │
    │ - Закрыть браузер             │
    └────────┬──────────────────────┘
             │
             ↓
    ┌────────────────────────────┐
    │ Любое действие вызывает:   │
    │ setRecipes(newData)        │
    │ localStorage.setItem(...)  │
    └────────┬───────────────────┘
             │
             ↓
    ┌────────────────────────────┐
    │ Данные сохранены в         │
    │ localStorage браузера       │
    └────────┬───────────────────┘
             │
             ↓
    ┌────────────────────────────┐
    │ БРАУЗЕР ЗАКУПАЕТСЯ         │
    │ (Данные в localStorage      │
    │  остаются)                 │
    └────────┬───────────────────┘
             │
             ↓
    ┌────────────────────────────┐
    │ БРАУЗЕР ОТКРЫВАЕТСЯ СНОВА  │
    │ (спустя часы/дни)          │
    └────────┬───────────────────┘
             │
             ↓
    ┌────────────────────────────┐
    │ useEffect находит данные в │
    │ localStorage и восстанавл- │
    │ вает их                    │
    └────────┬───────────────────┘
             │
             ↓
    ┌────────────────────────────┐
    │ ✅ ВСЕ РЕЦЕПТЫ ВСЕ ЕЩЕ ТУТ!│
    └────────────────────────────┘
```

## Структура файлов и связи

```
app/admin/recipes/
└── page.tsx  (RecipesPage component)
    │
    ├─ STATE:
    │  └─ recipes[] ↔️ localStorage["recipes"]
    │
    ├─ HANDLERS:
    │  ├─ handleCreateRecipe()  → setRecipes() + localStorage.setItem()
    │  ├─ handleEditRecipe()    → setRecipes() + localStorage.setItem()
    │  ├─ handleDeleteRecipe()  → setRecipes() + localStorage.setItem()
    │  └─ filterRecipes()       → setFilteredRecipes()
    │
    └─ COMPONENTS:
       ├─ RecipeWizard
       │  └─ onSubmit → handleCreateRecipe()
       │
       ├─ RecipePreviewCard (для каждого рецепта)
       │  ├─ onClick → RecipeDetailsModal (просмотр)
       │  ├─ Edit → handleEditClick() → RecipeEditModal
       │  └─ Delete → handleDeleteRecipe()
       │
       ├─ RecipeEditModal
       │  └─ onSubmit → handleEditRecipe()
       │
       └─ RecipeAIGenerator
          └─ onGenerate → handleCreateRecipe()
```

## Состояния рецепта в базе

```
STATE: recipes[]                    LOCALSTORAGE: localStorage["recipes"]
┌──────────────────┐               ┌──────────────────────┐
│ Recipe {         │               │ JSON String:         │
│   id,            │               │ "[                   │
│   name,          │ ─────────────→│   {                  │
│   description,   │   serialize() │     \"id\": \"123\",  │
│   ingredients,   │               │     \"name\": \"...\",│
│   instructions,  │               │     ...              │
│   images,        │               │   }                  │
│   youtubeUrl,    │               │ ]"                   │
│   ...            │               └──────────────────────┘
│ }                │
└──────────────────┘
```

## Блок-схема handleCreateRecipe()

```
START: handleCreateRecipe(recipeData)
  │
  ├─ Создать объект Recipe
  │  ├─ id: String(Date.now())     ← Уникальный ID
  │  ├─ name: recipeData.name
  │  ├─ ingredients: recipeData.ingredients || []
  │  ├─ instructions: recipeData.instructions || []
  │  ├─ images: recipeData.images || []
  │  ├─ youtubeUrl: recipeData.youtubeUrl || ""
  │  └─ ... все остальные поля с значениями по умолчанию
  │
  ├─ updatedRecipes = [newRecipe, ...recipes]
  │
  ├─ localStorage.setItem("recipes", JSON.stringify(updatedRecipes))
  │
  ├─ setRecipes(updatedRecipes)  ← Обновляет STATE
  │
  ├─ filterRecipes()             ← Фильтрует результаты
  │
  ├─ setShowCreateModal(false)   ← Закрывает диалог
  │
  ├─ alert("✅ Рецепт создан")   ← Уведомляет пользователя
  │
END
```

---

**Ключевое понимание:**
- 🔄 STATE синхронизируется с localStorage
- 💾 Каждое изменение сохраняется немедленно
- 🔙 При перезагрузке STATE восстанавливается из localStorage
- ✅ Данные сохраняются между сеансами браузера
