# 📁 Структура проекта - Modern Food Academy

> Обновлено: 4 января 2026 г.  
> Архитектура: 3-зонная (Public / User App / Admin Panel)

---

## 🏗️ Верхнеуровневая структура

```
cv-sushi_chef/
│
├── 📂 app/                    # Next.js App Router
│   ├── 🔓 (root)              # Публичная зона
│   ├── 👤 app/                # Пользовательское приложение
│   ├── 🛡️ admin/              # Админ-панель
│   └── 📡 api/                # API Routes
│
├── 📂 components/             # React компоненты
│   ├── admin/                 # Админ-компоненты
│   ├── auth/                  # Авторизация
│   ├── layout/                # Навигация и лейауты
│   ├── profile/               # Профиль
│   ├── recipes/               # Рецепты
│   └── ui/                    # UI библиотека (shadcn)
│
├── 📂 contexts/               # React Context (state management)
│   ├── AuthContext.tsx        # Auth + роли + cookies
│   ├── UserContext.tsx        # Данные пользователя
│   ├── LanguageContext.tsx    # i18n
│   └── ...
│
├── 📂 lib/                    # Утилиты и хелперы
│   ├── admin/                 # Админ-логика
│   ├── api/                   # API клиент
│   ├── i18n/                  # Интернационализация
│   └── ...
│
├── 📂 docs/                   # Документация
│   ├── ARCHITECTURE_ROUTING.md    # 3-зонная архитектура
│   ├── ADMIN_AUTO_REDIRECT.md     # Auth flow
│   └── ...
│
├── 📄 middleware.ts           # Role-based routing
├── 📄 next.config.ts          # Next.js конфигурация
├── 📄 tailwind.config.ts      # Tailwind CSS
└── 📄 package.json
```

---

## 🔓 Публичная зона (`/`)

**Доступ:** Все (гости, пользователи, админы)  
**Навигация:** `NavigationBurger` (упрощённое меню)

```
app/
├── page.tsx                   # 🏠 Главная страница (лендинг)
├── layout.tsx                 # Корневой layout
├── globals.css                # Глобальные стили
│
├── 📂 academy/ (public)       # 📚 Публичная академия (превью)
│   └── ...
│
├── 📂 pricing/                # 💰 Цены
├── 📂 about/                  # ℹ️ О нас
└── 📂 how-it-works/           # ❓ Как это работает
```

**Меню для гостей:**
```
✅ Главная
✅ Як це працює
✅ Академія (превью)
✅ Ціни
✅ Увійти
❌ Холодильник, AI, Токени, Админка
```

---

## 👤 Пользовательское приложение (`/app`)

**Доступ:** Только авторизованные пользователи (не админы)  
**Layout:** `app/app/layout.tsx` (UserLayout)  
**Навигация:** `components/layout/UserNavigation.tsx`  
**Middleware:** Редирект на `/` если не авторизован

```
app/app/
├── page.tsx                   # 👤 User Dashboard
├── layout.tsx                 # UserLayout (с UserNavigation)
│
├── 📂 fridge/                 # 🧊 Холодильник
│   └── page.tsx               # Управление продуктами
│
├── 📂 recipes/                # 📖 Рецепты
│   ├── page.tsx               # Каталог рецептов
│   ├── [id]/                  # Детали рецепта
│   │   ├── page.tsx
│   │   └── cook/              # Режим готовки
│   └── saved/                 # ⭐ Мои рецепты
│       └── page.tsx
│
├── 📂 assistant/              # 🤖 AI Ассистент
│   └── page.tsx               # Умная кухонная помощь
│
├── 📂 market/                 # 🛒 Маркет
│   ├── page.tsx               # Купить/продать рецепты
│   └── [id]/
│       └── page.tsx
│
├── 📂 academy/                # 🎓 Академия
│   ├── page.tsx               # Главная академии
│   ├── courses/               # Курсы
│   │   ├── page.tsx
│   │   └── [id]/
│   ├── paths/                 # Образовательные пути
│   │   └── [pathId]/
│   ├── community/             # Сообщество
│   ├── feed/                  # Лента
│   ├── leaderboard/           # Таблица лидеров
│   ├── tasks/                 # Задания
│   ├── earn-tokens/           # Заработок токенов
│   └── certificates/          # Сертификаты
│
├── 📂 tokens/                 # 💰 ChefTokens
│   └── page.tsx               # Управление токенами
│
├── 📂 losses/                 # 📉 Потери
│   └── page.tsx               # Анализ потерь продуктов
│
├── 📂 profile/                # 👤 Профиль
│   ├── page.tsx               # Мой профиль
│   ├── settings/              # ⚙️ Настройки
│   └── [id]/                  # Профиль другого пользователя
│       └── page.tsx
│
└── 📂 cheftokens/             # 🔄 Редирект
    └── page.tsx               # Старый URL → /app/tokens
```

**Меню пользователя:**
```
🍳 КУХНЯ
  ├─ Холодильник        /app/fridge
  ├─ Рецепти            /app/recipes
  ├─ AI Асистент        /app/assistant
  ├─ Мої рецепти        /app/recipes/saved
  └─ Маркет             /app/market

📚 РОЗВИТОК
  └─ Академія           /app/academy

💰 ЕКОНОМІКА
  └─ Токени             /app/tokens

👤 ПРОФІЛЬ
  └─ Мій профіль        /app/profile
```

---

## 🛡️ Админ-панель (`/admin`)

**Доступ:** Только `admin` и `superadmin`  
**Layout:** `app/admin/layout.tsx` (AdminLayout)  
**Навигация:** `components/admin/AdminSidebar.tsx` (Shadcn Sidebar)  
**Middleware:** Редирект на `/app` если не админ

```
app/admin/
├── page.tsx                   # 📊 Admin Dashboard
├── layout.tsx                 # AdminLayout (с AdminSidebar)
│
├── 📂 dashboard/              # 📊 Главная админки
│   └── page.tsx               # KPI, статистика
│
├── 📂 users/                  # 👥 Управление пользователями
│   └── page.tsx               # Список пользователей
│
├── 📂 recipes/                # 📖 Управление рецептами
│   ├── page.tsx               # Модерация рецептов
│   └── create/                # Создать рецепт
│       └── page.tsx
│
├── 📂 courses/                # 🎓 Управление курсами
│   ├── page.tsx
│   └── create/
│       └── page.tsx
│
├── 📂 orders/                 # 🛒 Заказы
│   └── page.tsx
│
├── 📂 activity-log/           # 📈 Лог активности
│   └── page.tsx               # Аудит действий
│
├── 📂 token-bank/             # 💰 Банк токенов
│   └── page.tsx               # Управление экономикой
│
├── 📂 integrations/           # 🔌 Интеграции
│   └── page.tsx               # Внешние сервисы
│
└── 📂 settings/               # ⚙️ Настройки системы
    └── page.tsx
```

**Меню админа:**
```
📊 Dashboard          /admin

👥 КОРИСТУВАЧІ
  └─ Користувачі      /admin/users

📖 КОНТЕНТ
  ├─ Рецепти          /admin/recipes
  ├─ Курси            /admin/courses
  └─ Замовлення       /admin/orders

🤖 AI & ЛОГІКА
  └─ Активність       /admin/activity-log

💰 ЕКОНОМІКА
  └─ Банк токенів     /admin/token-bank

🔧 ОПЕРАЦІЇ
  └─ Інтеграції       /admin/integrations

⚙️ СИСТЕМА
  └─ Налаштування     /admin/settings
```

---

## 📡 API Routes (`/app/api`)

**Структура:**

```
app/api/
│
├── 📂 auth/                   # 🔐 Авторизация
│   ├── login/
│   ├── register/
│   ├── logout/
│   └── me/
│
├── 📂 user/                   # 👤 Профиль пользователя
│   ├── profile/
│   ├── tokens/
│   ├── recipes/
│   └── language/
│
├── 📂 fridge/                 # 🧊 Холодильник
│   ├── items/                 # CRUD продуктов
│   ├── add-missing/           # Добавить недостающие
│   ├── deduct/                # Списать продукты
│   └── test/
│
├── 📂 recipes/                # 📖 Рецепты
│   ├── [id]/                  # Получить рецепт
│   ├── available/             # Доступные рецепты
│   ├── match/                 # Поиск по ингредиентам
│   └── recommendations/       # Рекомендации
│
├── 📂 ai/                     # 🤖 AI endpoints
│   ├── fridge/                # AI для холодильника
│   ├── create-recipe-from-fridge/
│   └── recalculate-recipe-economy/
│
├── 📂 market/                 # 🛒 Маркет
│   └── recipes/               # Рецепты на продажу
│
├── 📂 academy/                # 🎓 Академия
│   └── ai/                    # AI для обучения
│
├── 📂 admin/                  # 🛡️ Админ API
│   ├── token-bank/
│   └── treasury/
│
├── 📂 catalog/                # 📚 Справочники
│   └── ingredients/           # Каталог ингредиентов
│
├── 📂 token-bank/             # 💰 Токены
│   └── me/
│
├── 📂 public/                 # 🔓 Публичные API
│   └── treasury/
│
├── 📂 settings/               # ⚙️ Настройки
├── 📂 tasks/                  # 📋 Задания
└── 📂 generate-recipe/        # 🎲 Генерация рецепта
```

---

## 🧩 Компоненты (`/components`)

```
components/
│
├── 📂 admin/                  # 🛡️ Админ-компоненты
│   ├── AdminSidebar.tsx       # Боковое меню админки
│   └── ...
│
├── 📂 auth/                   # 🔐 Авторизация
│   ├── AuthModal.tsx          # Модалка входа/регистрации
│   ├── GlobalAuthModal.tsx    # Глобальная обёртка
│   ├── AuthGate.tsx           # Проверка auth
│   └── TokenValidator.tsx     # Валидация токена
│
├── 📂 layout/                 # 🎨 Навигация
│   ├── UserNavigation.tsx     # Меню пользователя (/app)
│   └── ...
│
├── 📂 profile/                # 👤 Профиль
│   ├── ProfileHeader.tsx
│   ├── StatsCard.tsx
│   └── ...
│
├── 📂 recipes/                # 📖 Рецепты
│   ├── RecipeCard.tsx
│   ├── RecipeGrid.tsx
│   └── ...
│
├── 📂 fridge/                 # 🧊 Холодильник
│   └── ...
│
├── 📂 ui/                     # 🎨 UI библиотека (shadcn)
│   ├── button.tsx
│   ├── card.tsx
│   ├── sidebar.tsx
│   └── ...
│
├── 📂 theme/                  # 🌓 Темы
│   └── ThemeProvider.tsx
│
├── NavigationBurger.tsx       # 🔓 Публичное меню
├── CartIcon.tsx               # 🛒 Корзина
└── NotificationBell.tsx       # 🔔 Уведомления
```

---

## 🔧 Contexts (`/contexts`)

```
contexts/
├── AuthContext.tsx            # 🔐 Авторизация + роли + cookies
├── UserContext.tsx            # 👤 Данные пользователя
├── LanguageContext.tsx        # 🌍 i18n (EN/RU/PL)
├── SettingsContext.tsx        # ⚙️ Настройки приложения
├── RecipeContext.tsx          # 📖 Рецепты
├── CartContext.tsx            # 🛒 Корзина
└── NotificationContext.tsx    # 🔔 Уведомления
```

---

## 🛠️ Библиотеки (`/lib`)

```
lib/
│
├── 📂 admin/                  # 🛡️ Админ-утилиты
│   └── navigation-schema.ts   # Навигация админки (RBAC)
│
├── 📂 api/                    # 📡 API клиент
│   ├── base.ts                # Базовый HTTP клиент
│   ├── auth.ts                # Auth endpoints
│   ├── user.ts                # User endpoints
│   └── ...
│
├── 📂 i18n/                   # 🌍 Интернационализация
│   ├── constants.ts           # Языки: EN, RU, PL
│   ├── getDictionary.ts       # Загрузка переводов
│   └── types.ts               # TypeScript типы
│
├── 📂 hooks/                  # 🪝 Custom hooks
│   ├── useTranslations.ts
│   ├── useRecipeGeneration.ts
│   └── ...
│
└── utils.ts                   # 🔧 Утилиты
```

---

## 📚 Документация (`/docs`)

```
docs/
├── ARCHITECTURE_ROUTING.md        # 🏗️ 3-зонная архитектура
├── ADMIN_AUTO_REDIRECT.md         # 🔐 Auth flow и редиректы
├── ADMIN_ARCHITECTURE.md          # 🛡️ Админ-панель
├── I18N_SYSTEM.md                 # 🌍 Система i18n
├── API_CONTRACT_GUIDE.md          # 📡 API контракты
├── SETTINGS_SYSTEM.md             # ⚙️ Система настроек
└── ...
```

---

## 🔐 Middleware (`middleware.ts`)

**Функционал:**
1. ✅ **i18n** - язык из cookies
2. ✅ **Role-based routing** - защита маршрутов

**Правила:**
```typescript
// Гость
if (!token && (pathname.startsWith('/app') || pathname.startsWith('/admin'))) {
  redirect('/');
}

// Админ
if (role === 'admin' && pathname.startsWith('/app')) {
  redirect('/admin');
}

// User
if (token && role !== 'admin' && pathname.startsWith('/admin')) {
  redirect('/app');
}
```

---

## 📊 Статистика

- **Всего папок:** 79+
- **Зоны:** 3 (Public, User App, Admin Panel)
- **API endpoints:** 30+
- **Компоненты:** 100+
- **Контексты:** 7
- **Языки:** 3 (EN, RU, PL)

---

## 🎯 Ключевые файлы

| Файл | Описание |
|------|----------|
| `middleware.ts` | Role-based routing + i18n |
| `app/layout.tsx` | Корневой layout (все провайдеры) |
| `app/app/layout.tsx` | User layout (UserNavigation) |
| `app/admin/layout.tsx` | Admin layout (AdminSidebar) |
| `contexts/AuthContext.tsx` | Auth + роли + cookies |
| `components/layout/UserNavigation.tsx` | Меню пользователя |
| `components/admin/AdminSidebar.tsx` | Меню админа |
| `lib/admin/navigation-schema.ts` | RBAC для админки |

---

## ✅ Текущий статус

**Прогресс: 6/8 задач выполнено**

- ✅ Middleware с role-based routing
- ✅ UserLayout создан
- ✅ AuthContext обновлён (cookies + редиректы)
- ✅ UserNavigation создан
- ✅ Страницы перемещены в `/app/app`
- ✅ Документация обновлена
- ⏳ Landing Navigation (для `/`)
- ⏳ Рефакторинг AdminSidebar

**Архитектура:** 🟢 MVP → Продукт (готова!)
