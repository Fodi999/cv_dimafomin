# 🔐 Admin Panel - Личный кабинет администратора

## Структура папки

```
/app/admin/
├── layout.tsx          # Главный layout с навигацией
├── page.tsx            # Главная страница со статистикой
├── users/
│   └── page.tsx        # Управление пользователями
├── orders/
│   └── page.tsx        # Управление заказами
└── settings/
    └── page.tsx        # Настройки панели
```

## 📊 Функциональность

### 1. **Dashboard (Главная страница)**
- Статистика: всего пользователей, заказов, доход, активные пользователи
- Последние заказы в таблице
- Быстрые ссылки на основные разделы

### 2. **Управление пользователями** (`/admin/users`)
- Список всех пользователей
- Поиск по имени и email
- Изменение роли (user, instructor, admin)
- Удаление пользователей
- Просмотр статуса активности

### 3. **Управление заказами** (`/admin/orders`)
- Список всех заказов
- Фильтрация по статусу (pending, completed, cancelled)
- Поиск по имени, email, ID заказа
- Изменение статуса заказа
- Просмотр сумм и информации о клиентах

### 4. **Настройки** (`/admin/settings`)
- Уведомления (Email, Push)
- Интерфейс (Тёмный режим)
- Данные (Резервное копирование)
- Безопасность (Пароль, 2FA)
- Информация об учетной записи

## 🔒 Безопасность

- ✅ Проверка роли `admin` в layout
- ✅ Bearer token аутентификация
- ✅ Защита от несанкционированного доступа
- ✅ Редирект на главную если пользователь не админ

## 🎨 Дизайн

- Dark mode поддержка
- Responsive дизайн (мобильный, планшет, десктоп)
- Collectible sidebar (сворачивается/разворачивается)
- Gradient иконки и кнопки
- Smooth анимации и переходы

## 📡 API Endpoints

Админ панель использует следующие endpoints (из вашего backend):

### Статистика
```bash
GET /api/admin/stats
Headers: Authorization: Bearer $TOKEN
```

### Пользователи
```bash
GET /api/admin/users                    # Получить всех пользователей
GET /api/admin/users/:id                # Получить пользователя
PUT /api/admin/users/:id/role           # Изменить роль
DELETE /api/admin/users/:id             # Удалить пользователя
```

### Заказы
```bash
GET /api/admin/orders                   # Получить все заказы
GET /api/admin/orders/:id               # Получить заказ
PUT /api/admin/orders/:id               # Обновить статус заказа
```

## 🚀 Использование

### 1.访问админ панели
```
http://localhost:3000/admin
```

Требует аутентификации и роли `admin`

### 2. Страницы
- **Статистика**: `/admin` - Главный dashboard
- **Пользователи**: `/admin/users` - Управление юзерами
- **Заказы**: `/admin/orders` - Управление заказами
- **Настройки**: `/admin/settings` - Конфиг панели

## 📦 Зависимости

- Next.js 13+ (App Router)
- React 18
- Tailwind CSS
- Framer Motion (опционально для анимаций)
- Lucide Icons (иконки)

## 🔧 Установка

1. Убедитесь, что backend админ API работает:
```bash
# На backend машине
./run-backend.sh
```

2. Запустите frontend:
```bash
npm run dev
```

3. Откройте браузер:
```
http://localhost:3000/admin
```

## ✨ Возможные улучшения

- [ ] Экспорт данных (CSV, PDF)
- [ ] График статистики (Chart.js/Recharts)
- [ ] Система логирования действий админа
- [ ] Расширенный поиск и фильтры
- [ ] Пакетные операции (bulk delete/update)
- [ ] Email рассылки
- [ ] Управление промоционными кодами
- [ ] Analytics dashboard

## 🐛 Troubleshooting

**Ошибка: "401 Unauthorized"**
- Проверьте, что токен валиден в localStorage
- Убедитесь, что пользователь имеет роль `admin`

**Ошибка: "Не удается получить данные"**
- Проверьте, что backend запущен и доступен
- Посмотрите консоль браузера для деталей ошибки
- Убедитесь, что CORS настроен на backend

**Статистика не загружается**
- Проверьте endpoint `/api/admin/stats`
- Убедитесь, что Authorization header правильный
- Посмотрите logs на backend

## 📝 Автор

Дима Фомин (dmitrijfomin@gmail.com)
