# 💰 ChefTokens Wallet Feature

## Огляд
Гаманець ChefTokens - це система винагород та транзакцій для студентів Culinary Academy. Учні можуть заробляти токени за виконання завдань та витрачати їх на курси, сертифікати та інші продукти.

## Компоненти

### 1. WalletCard Component
**Файл:** `/components/academy/WalletCard.tsx`

**Основні можливості:**
- ✅ **Акордеон UI** - Компактний вигляд з можливістю розгортання
- ✅ Відображення поточного балансу в заголовку акордеону
- ✅ Статистика: всього зароблено / витрачено
- ✅ Історія останніх 10 транзакцій
- ✅ Анімовані картки з hover ефектами
- ✅ Smooth анімації відкриття/закриття (Framer Motion)
- ✅ Підтримка польської (pl) та української (ua) мов
- ✅ Адаптивний дизайн (mobile-first)

**Props:**
```typescript
interface WalletCardProps {
  balance: number;           // Поточний баланс токенів
  totalEarned: number;       // Всього зароблено за весь час
  totalSpent: number;        // Всього витрачено за весь час
  transactions?: Transaction[]; // Масив транзакцій (опціонально)
}
```

**Стан компонента:**
- `isOpen: boolean` - Стан акордеону (відкрито/закрито)
- За замовчуванням: `false` (закрито)

### 2. Transaction Type
**Файл:** `/lib/types.ts`

```typescript
interface Transaction {
  id: string;                  // Унікальний ID транзакції
  type: 'earned' | 'spent';    // Тип: зароблено або витрачено
  amount: number;              // Сума токенів
  reason: string;              // Причина (ключ для перекладу)
  date: string;                // ISO дата транзакції
}
```

### 3. Dashboard Integration
**Файл:** `/app/academy/dashboard/page.tsx`

Гаманець відображається після блоку Level Progress:
- Дані беруться з `dashboardData.wallet` (API response)
- Якщо wallet відсутній в API - використовуються mock дані
- Mock транзакції показують приклад функціоналу

## Типи транзакцій

### Заробіток токенів (earned):
- `courseCompletion` - Завершення курсу (+100 CT)
- `lessonComplete` - Завершення уроку (+50 CT)
- `achievement` - Досягнення (+25 CT)
- `dailyBonus` - Щоденний бонус (+10 CT)
- `referral` - Запрошення друга (+50 CT)

### Витрати токенів (spent):
- `coursePurchase` - Покупка курсу (-75 CT)
- `certificatePurchase` - Покупка сертифікату (-50 CT)
- `marketplace` - Покупка в магазині (варіюється)

## Переклади

### Структура перекладів
**Файл:** `/lib/translations.ts`

```typescript
academy: {
  dashboard: {
    wallet: {
      title: string;           // "Гаманець ChefTokens" / "Portfel ChefTokens"
      balance: string;         // "Баланс" / "Saldo"
      totalEarned: string;     // "Всього зароблено"
      totalSpent: string;      // "Всього витрачено"
      recentTransactions: string;
      noTransactions: string;
      infoNote: string;        // Підказка як заробляти токени
      type: {
        earned: string;
        spent: string;
      },
      reasons: {
        courseCompletion: string;
        lessonComplete: string;
        achievement: string;
        dailyBonus: string;
        referral: string;
        coursePurchase: string;
        certificatePurchase: string;
        marketplace: string;
      }
    }
  }
}
```

## Backend API Integration

### Endpoint для отримання даних гаманця:
```
GET /api/academy/users/{userId}/dashboard
```

### Response structure:
```json
{
  "wallet": {
    "chefTokens": 110,
    "totalEarned": 185,
    "totalSpent": 75,
    "transactions": [
      {
        "id": "uuid-123",
        "type": "earned",
        "amount": 100,
        "reason": "courseCompletion",
        "date": "2025-11-04T10:30:00Z"
      }
    ]
  }
}
```

## Візуальний дизайн

### Кольорова схема:
- **Баланс:** Градієнт amber-400 → orange-600
- **Зароблено:** Градієнт green-400 → emerald-600  
- **Витрачено:** Градієнт red-400 → rose-600
- **Транзакції earned:** Зелений фон (bg-green-50)
- **Транзакції spent:** Червоний фон (bg-red-50)

### Іконки (Lucide React):
- `Coins` - Головна іконка гаманця
- `ChevronDown` - Іконка акордеону (обертається при відкритті)
- `TrendingUp` - Всього зароблено
- `TrendingDown` - Всього витрачено
- `ArrowUpRight` - Транзакція заробітку
- `ArrowDownRight` - Транзакція витрат
- `Calendar` - Історія транзакцій

## Animations (Framer Motion)

### Accordion Animations:
- **Header chevron:** `rotate: 0 → 180` при відкритті
- **Content expand:** `height: 0 → auto`, `opacity: 0 → 1`
- **Duration:** 0.3s з `easeInOut` easing

### Card Animations:
- **Balance cards:** Staggered appear з delay (0.1s, 0.2s, 0.3s)
- **Hover на картках:** `scale: 1.02` 
- **Transactions:** Fade-in з `y: 10 → 0` і staggered delay (0.05s між елементами)
- **Initial scale:** `0.9 → 1` для карток статистики

## Responsive Design

### Mobile (default):
- 1 колонка для всіх карток
- Компактний відступ (p-6)
- Менший розмір тексту

### Desktop (md:):
- 3 колонки для карток статистики
- Більший відступ (p-8)
- Великий шрифт для балансу (text-5xl)

## Mock Data (для тестування)

Якщо API не повертає транзакції, використовуються mock дані:
- 5 зразкових транзакцій за останні 5 днів
- Баланс: розраховується з wallet.chefTokens або profile.chefTokens
- Total Earned: 185 CT (за замовчуванням)
- Total Spent: 75 CT (за замовчуванням)

## Майбутні покращення

- [x] Акордеон UI для компактного відображення
- [ ] Запам'ятовування стану акордеону (localStorage)
- [ ] Фільтр транзакцій за типом (earned/spent)
- [ ] Фільтр за датою (останній тиждень, місяць, рік)
- [ ] Пагінація для історії транзакцій
- [ ] Експорт історії в CSV/PDF
- [ ] Графіки заробітку та витрат (Chart.js)
- [ ] Push-сповіщення про нові токени
- [ ] Реферальна система з bonus токенами

## Використання Accordion

### Відкриття/Закриття:
```tsx
const [isOpen, setIsOpen] = useState(false);

<button onClick={() => setIsOpen(!isOpen)}>
  {/* Accordion header */}
</button>
```

### Структура:
1. **Header (завжди видимий):**
   - Іконка гаманця
   - Назва "Гаманець ChefTokens"
   - Поточний баланс
   - Chevron іконка (обертається)

2. **Content (розгортається):**
   - 3 статистичні картки
   - Історія транзакцій
   - Інформаційна підказка

## Налаштування

Для зміни кількості відображуваних транзакцій:
```tsx
{transactions.slice(0, 10).map((transaction) => (
  // Змініть 10 на потрібну кількість
))}
```

## Troubleshooting

### Проблема: Не відображаються транзакції
**Рішення:** Перевірте чи API повертає `wallet.transactions` масив

### Проблема: Неправильний формат дати
**Рішення:** Переконайтесь що дата в ISO форматі (ISO 8601)

### Проблема: Переклади не працюють
**Рішення:** Перевірте `useLanguage()` контекст та наявність ключів в translations.ts

---

**Автор:** Dima Fomin  
**Дата створення:** 5 листопада 2025  
**Версія:** 1.0.0
