# 🔧 API 404 Error - Mock Data Fallback

## ❌ Проблема

```
❌ API Error 404: {}
at apiFetch (lib/api.ts:85:13)
at async ProfilePage.useEffect.loadData (app/profile/dashboard/page.tsx:44:45)
```

Backend возвращает **404** при попытке загрузить:
- `GET /api/user/profile`
- `GET /api/user/wallet`

## ✅ Решение

Добавлена **mock data** для разработки. Когда API падает с 404, страница показывает демо-данные вместо ошибки.

### 🎯 Как работает

```typescript
// 1. Попытаться загрузить с API
const profileData = await userApi.getProfile(token).catch(err => {
  console.warn("Ошибка API:", err);
  return null;  // Вернуть null при ошибке
});

// 2. Если null - использовать mock
const finalProfile = profileData || mockProfile;

// 3. Установить данные
setProfile(finalProfile);
```

### 📊 Mock Data

```typescript
const mockProfile: UserProfile = {
  name: "Демо Пользователь",
  email: "demo@example.com",
  avatarUrl: "https://api.dicebear.com/7.x/avataaars/svg?seed=demo",
  bio: "Профессиональный шеф-повар 👨‍🍳",
  location: "Москва, Россия 🇷🇺",
  level: 5,
  xp: 2450,
  maxXp: 5000,
  chefTokens: 1500,
  coursesCount: 3,
  followers: 42,
  following: 28,
};

const mockWallet = {
  chefTokens: { balance: 1500, currency: "tokens" },
  paymentMethods: [],
  purchases: [],
  subscriptions: [],
  totalSpent: 5000,
  totalEarnings: 8500,
  nextPaymentDate: "2024-01-15",
};
```

## 🎯 Что это даёт

| До | После |
|----|-------|
| ❌ 404 ошибка | ✅ Показывает демо-профиль |
| ❌ Белый экран | ✅ Видно все компоненты |
| ❌ Неработающий dashboard | ✅ Можно тестировать UI |
| ❌ Неясно что вызывает ошибку | ✅ Видно точное сообщение в консоли |

## 📝 Логи в консоли

Когда API падает, в Console видно:

```
📡 API Call: GET https://yeasty-madelaine-fodi999-671ccdf5.koyeb.app/api/user/profile
❌ API Error 404: {
  endpoint: "/user/profile",
  method: "GET",
  status: 404,
  message: "Not Found"
}
⚠️ [ProfileDashboard] Ошибка загрузки профиля (используем mock)
📌 [ProfileDashboard] Используем mock data
✅ Профиль загружен с mock данными
```

## 🔄 Как исправить на backend

Когда backend будет готов, просто добавьте endpoints:

```go
// Go код (примерно)
GET /api/user/profile → вернуть профиль пользователя
GET /api/user/wallet → вернуть данные кошелька
```

И фронт **автоматически** начнёт использовать реальные данные!

## 🧪 Как тестировать

1. **С mock data** (сейчас):
   ```
   http://localhost:3000/profile/dashboard
   → Видно демо-профиль
   → В консоли 404 ошибка
   ```

2. **После того как backend готов**:
   ```
   http://localhost:3000/profile/dashboard
   → Видно реальный профиль пользователя
   → В консоли успешные запросы
   ```

## ✅ Улучшения которые были внесены

- ✅ Mock data для разработки
- ✅ Graceful error handling (не падает на 404)
- ✅ Подробные логи в консоли
- ✅ Fallback к mock когда API недоступен
- ✅ Автоматический переход на реальные данные когда API готов

## 🔗 Файлы которые изменились

- `/app/profile/dashboard/page.tsx` - добавлена mock data и улучшена обработка ошибок

---

**Status:** ✅ ГОТОВО К РАЗРАБОТКЕ  
**Next Step:** Когда backend endpoints будут готовы, просто убедитесь что они возвращают правильный формат данных
