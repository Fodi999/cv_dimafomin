# 🚀 ChefOS - Quick Start Guide (2026)

## 📌 Что изменилось?

ChefOS теперь имеет **2 режима работы** в одном приложении:

### 🔐 **Admin Mode** (владелец бизнеса)
- Управление складом ингредиентов
- Создание рецептов (BOM)
- Карточки товаров на продажу
- Экономика и списания
- Управление заказами
- AI Assistant для бизнеса

### 🟢 **Customer Mode** (покупатель)
- Marketplace (каталог блюд)
- Мои заказы
- Профиль

---

## 🔑 Роли и доступ

| Роль | Backend | Frontend | Режим | Доступ |
|------|---------|----------|-------|--------|
| `superadmin` | `role: "superadmin"` | `super_admin` | `admin` | Полный (Admin + Customer) |
| `admin` (старая) | `role: "admin"` | `customer` | `customer` | Customer Mode |
| Любая другая | `role: "user"` | `customer` | `customer` | Customer Mode |

---

## 🛠️ Как использовать

### 1. Создать super admin (в БД)
```sql
UPDATE users SET role = 'superadmin' WHERE email = 'your-email@example.com';
```

### 2. Войти в систему
- Super admin → автоматически попадает в `/admin`
- Обычный пользователь → автоматически попадает в `/customer/marketplace`

### 3. Навигация

#### Admin (Desktop)
- Sidebar слева с разделами
- Dashboard, Ingredients, Recipes, Products, Orders, Economy, AI Assistant

#### Admin (Mobile)
- Header вверху
- Bottom Navigation (4 кнопки)

#### Customer (Desktop + Mobile)
- Clean Header
- Bottom Navigation: Marketplace, Orders, Profile

---

## 📂 Структура проекта

```
app/
├── admin/                    # 🔐 Admin Mode
│   ├── layout.tsx           # Guard + Sidebar
│   ├── page.tsx             # Dashboard
│   ├── ingredients/         # Склад (ex-Fridge)
│   ├── recipes/             # Рецепты (BOM)
│   ├── products/            # Товары
│   ├── orders/              # Заказы
│   ├── economy/             # Экономика
│   └── assistant/           # AI Assistant
│
├── customer/                 # 🟢 Customer Mode
│   ├── layout.tsx           # Guard + Minimal Nav
│   ├── marketplace/         # Каталог блюд
│   ├── orders/              # История заказов
│   └── profile/             # Профиль
│
contexts/
├── SessionContext.tsx       # ✅ NEW (ex-UserContext)
└── UserContext.tsx          # ⚠️ DEPRECATED
```

---

## 🔄 Миграция существующего кода

### Использование SessionContext

```typescript
// ✅ Новый способ
import { useSession } from "@/contexts/SessionContext";

const { session, user, isAdmin } = useSession();

if (isAdmin) {
  // Логика для админа
}

// ✅ Обратная совместимость (alias)
import { useUser } from "@/contexts/SessionContext";

const { user } = useUser();
// Работает как раньше!
```

### Проверка роли

```typescript
// ✅ Правильно
if (session?.role === 'super_admin') {
  // Admin-only код
}

// ✅ Быстрая проверка
if (isAdmin) {
  // Admin-only код
}

// ❌ НЕ делать
if (user?.role === 'admin') {
  // Старая логика, больше не работает
}
```

---

## 🎯 Use Cases

### UC-1: Добавить ингредиент на склад (Admin)
1. Войти как super_admin
2. Перейти в `/admin/ingredients`
3. Нажать "Add Ingredient"
4. Выбрать продукт, количество, expiry date
5. Сохранить

### UC-2: Купить блюдо (Customer)
1. Войти как обычный пользователь
2. Открывается `/customer/marketplace`
3. Выбрать блюдо (TODO)
4. Добавить в корзину (TODO)
5. Оформить заказ (TODO)

### UC-3: Обработать заказ (Admin)
1. Уведомление о новом заказе
2. Перейти в `/admin/orders`
3. Проверить наличие ингредиентов
4. Списать ингредиенты со склада
5. Отметить заказ готовым
6. Посмотреть маржу (cost vs price)

---

## 🚧 В разработке

- [ ] Admin: Recipes (BOM)
- [ ] Admin: Products (карточки товаров)
- [ ] Admin: Orders (управление заказами)
- [ ] Admin: Economy (себестоимость, маржа)
- [ ] Customer: Marketplace (каталог блюд)
- [ ] Customer: Orders (история заказов)
- [ ] AI Assistant для Admin (бизнес-аналитика)

---

## 🐛 Troubleshooting

### Проблема: "Access denied" при входе в `/admin`
**Решение**: Проверьте роль в БД. Должна быть `superadmin`.

### Проблема: Редирект на `/customer/marketplace` вместо `/admin`
**Решение**: Роль пользователя != `super_admin`. Обновите в БД.

### Проблема: SessionContext undefined
**Решение**: Убедитесь, что `SessionProvider` обернут в `app/layout.tsx`.

---

## 📚 Документация

- `docs/ARCHITECTURE_MIGRATION_2026.md` - Полная миграция
- `docs/ARCHITECTURE_STATE_SEPARATION_DIAGRAM.md` - Диаграммы
- `docs/ARCHITECTURE_COOK_NOW_CONTRACT.md` - Контракты API

---

## ✅ Checklist для разработчика

### День 1 (Текущий)
- [x] SessionContext создан
- [x] Admin Layout создан
- [x] Customer Layout создан
- [x] Admin/Ingredients (ex-Fridge) создан
- [x] Customer/Marketplace (заглушка) создан
- [x] Документация написана

### День 2 (TODO)
- [ ] Перенести Admin/Recipes
- [ ] Перенести Admin/Economy/Losses
- [ ] Перенести Admin/Assistant
- [ ] Перенести Customer/Profile

### День 3 (TODO)
- [ ] Создать Admin/Products
- [ ] Создать Admin/Orders
- [ ] Интегрировать Backend API

---

**🎯 Цель**: B2B платформа для продажи готовых блюд с профессиональным управлением бизнесом.

**✅ Статус**: День 1 завершён. Можно тестировать!
