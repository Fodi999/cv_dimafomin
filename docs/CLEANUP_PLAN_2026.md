# 🧹 ПЛАН ОЧИСТКИ АРХИТЕКТУРЫ — 2026

**Дата:** 25 января 2026  
**Статус:** 📋 В процессе  
**Цель:** Устранить дублирование, привести к единой логике

---

## 🎯 ПРОБЛЕМЫ И РЕШЕНИЯ

### 1️⃣ ПРОБЛЕМА: `app/catalog/*` дублирует `customer/marketplace`

**Текущая ситуация:**
```
app/catalog/products/     ← ❌ Дублирование
app/catalog/recipes/      ← ❌ Дублирование
app/customer/marketplace/ ← ✅ Правильное место
```

**Решение:**
- [ ] Удалить `app/catalog/*` полностью
- [ ] Все маршруты `/catalog/*` → редирект на `/customer/marketplace`
- [ ] Обновить все ссылки в UI

**Причина:**
- `catalog` — это customer-термин (покупатели смотрят каталог)
- Не должно быть двух точек входа

---

### 2️⃣ ПРОБЛЕМА: `admin/catalog/*` дублирует `admin/products` и `admin/recipes`

**Текущая ситуация:**
```
admin/catalog/products/      ← ❌ Дублирование
admin/catalog/recipes-list/  ← ❌ Дублирование
admin/products/              ← ✅ Правильное место
admin/recipes/               ← ✅ Правильное место
```

**Решение:**
- [ ] Удалить `app/admin/catalog/*` полностью
- [ ] Убедиться, что navigation ведет на `/admin/products` и `/admin/recipes`
- [ ] Проверить, что нет прямых ссылок на `/admin/catalog`

**Причина:**
- `catalog` — не admin-термин
- Admin управляет конкретными сущностями: Products (товары), Recipes (BOM)

---

### 3️⃣ ПРОБЛЕМА: Старые файлы (`*-old.tsx`, `*.backup`)

**Найдено:**
```
app/admin/assistant/page-old.tsx           ← 20 файлов
app/api/admin/recipes/route.ts.backup      ← backup
app/api/admin/users/route.ts.backup
... (еще 17 файлов)
```

**Решение:**
- [ ] Удалить все `*-old.tsx`
- [ ] Удалить все `*.backup`
- [ ] Очистить когнитивную нагрузку

**Команда:**
```bash
find app -name "*old*" -delete
find app -name "*.backup" -delete
```

---

### 4️⃣ ПРОБЛЕМА: API доступы не зафиксированы

**Текущая ситуация:**
```
api/admin/*  — нет явной защиты в middleware
api/user/*   — используется и admin, и customer
```

**Решение:**
- [ ] Зафиксировать в `middleware.ts`:
  - `api/admin/**` → только `super_admin`
  - `api/user/**` → `customer` (НЕ admin)
- [ ] Документировать контракт в `API_ACCESS_MATRIX.md`

**Пример:**
```typescript
// middleware.ts
if (pathname.startsWith('/api/admin')) {
  if (role !== 'super_admin') return Response.redirect('/customer/marketplace');
}
```

---

### 5️⃣ ПРОБЛЕМА: Navigation может содержать перекрестные ссылки

**Что проверить:**
- [ ] `AdminNavigation` не ведет на `/customer/*`
- [ ] `CustomerNavigation` не ведет на `/admin/*`
- [ ] `NavigationBurger` правильно разделяет роли

**Файлы для проверки:**
```
components/admin/AdminNavigation.tsx
components/customer/CustomerNavigation.tsx  (если есть)
components/NavigationBurger.tsx
```

---

## 📋 ЧЕК-ЛИСТ ВЫПОЛНЕНИЯ

### Этап 1: Удаление дублирующих папок
- [ ] Удалить `app/catalog/*`
- [ ] Удалить `app/admin/catalog/*`
- [ ] Проверить сборку: `npm run build`

### Этап 2: Удаление старых файлов
- [ ] Удалить все `*-old.tsx`
- [ ] Удалить все `*.backup`
- [ ] Коммит: "chore: remove old and backup files"

### Этап 3: API Access Matrix
- [ ] Создать `docs/API_ACCESS_MATRIX.md`
- [ ] Обновить `middleware.ts` с явными проверками
- [ ] Протестировать доступы

### Этап 4: Navigation Audit
- [ ] Проверить `AdminNavigation` (только `/admin/*`)
- [ ] Проверить `NavigationBurger` (разделение по ролям)
- [ ] Обновить ссылки, если нужно

### Этап 5: Финальная проверка
- [ ] `npm run build` — 0 ошибок
- [ ] Grep-аудит: нет `/catalog/`, `/(user)/`
- [ ] Документация обновлена

---

## 🔍 ОЖИДАЕМАЯ СТРУКТУРА ПОСЛЕ ОЧИСТКИ

```
app/
├── (public)/           # Публичные страницы
├── admin/              # ADMIN ZONE
│   ├── dashboard/
│   ├── ingredients/    # Склад
│   ├── recipes/        # BOM
│   ├── products/       # Товары на продажу
│   ├── orders/         # Заказы B2B
│   ├── economy/        # Экономика
│   ├── assistant/      # AI
│   ├── users/
│   └── settings/
├── customer/           # CUSTOMER ZONE
│   ├── marketplace/    # ✅ ЕДИНСТВЕННЫЙ каталог
│   ├── orders/         # История заказов
│   └── profile/
└── api/
    ├── admin/          # Только super_admin
    └── user/           # Только customer
```

---

## 🚨 РИСКИ

### Низкий риск
- Удаление `*.backup` — это точно безопасно
- Удаление `*-old.tsx` — если они не импортируются

### Средний риск
- Удаление `app/catalog/*` — нужно проверить внешние ссылки
- Удаление `admin/catalog/*` — нужно обновить navigation

### Как минимизировать
1. Grep-поиск всех импортов
2. Проверка сборки после каждого этапа
3. Коммит после каждого успешного шага

---

## 📊 МЕТРИКИ ДО/ПОСЛЕ

| Метрика                  | До    | После |
|--------------------------|-------|-------|
| Папок в `app/`           | ~20   | ~15   |
| Точек входа в каталог    | 3     | 1     |
| Backup файлов            | 20    | 0     |
| Дублирующих routes       | 5     | 0     |

---

## 🎓 ПОЧЕМУ ЭТО ВАЖНО

1. **Когнитивная нагрузка**  
   Меньше папок → меньше путаницы

2. **Единая точка истины**  
   Один маршрут → легче поддерживать

3. **Безопасность**  
   Явные правила доступа → меньше ошибок

4. **Масштабируемость**  
   Чистая архитектура → проще добавлять фичи

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

После выполнения этого плана:
1. Обновить `MIGRATION_STAGE1_COMPLETE.md`
2. Создать `API_ACCESS_MATRIX.md`
3. Обновить README с новой структурой
4. Сделать финальный коммит: "feat: complete architecture cleanup"
