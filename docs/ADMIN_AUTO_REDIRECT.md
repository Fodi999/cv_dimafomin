# Admin Auto-Redirect After Login

## 📋 Обзор

Теперь после входа пользователи автоматически перенаправляются на соответствующую страницу в зависимости от их роли:

- **Admin / Superadmin** → `/admin/dashboard`
- **Обычные пользователи** → `/assistant`

## 🔧 Что изменилось

### 1. AuthContext (`/contexts/AuthContext.tsx`)

- ✅ `login()` теперь возвращает `Promise<string>` (URL редиректа)
- ✅ `register()` теперь возвращает `Promise<string>` (URL редиректа)
- ✅ Добавлена функция `getRedirectUrl(userRole: string): string`

```typescript
const getRedirectUrl = (userRole: string): string => {
  // Admin и Superadmin → admin panel
  if (userRole === "admin" || userRole === "superadmin") {
    return "/admin/dashboard";
  }
  
  // Обычные пользователи → главная страница (лендинг)
  return "/";
};
```

### 2. AuthModal (`/components/auth/AuthModal.tsx`)

- ✅ `handleLogin()` использует `redirectUrl` из `login()`
- ✅ `handleRegister()` использует `redirectUrl` из `register()`

```typescript
const redirectUrl = await login(loginForm.email, loginForm.password);
router.push(redirectUrl); // Редирект на правильный URL
```

### 3. UserContext (`/contexts/UserContext.tsx`)

- ✅ Добавлена роль `"superadmin"` в тип `User.role`

```typescript
role: "student" | "instructor" | "admin" | "superadmin" | "home_chef";
```

### 4. AdminLayout (`/app/admin/layout.tsx`)

- ✅ Проверка доступа теперь учитывает обе роли: `"admin"` и `"superadmin"`

```typescript
if (user.role !== "admin" && user.role !== "superadmin") {
  router.push("/"); // Редирект на главную, если не админ
}
```

### 5. Global Auth Modal (`/components/auth/GlobalAuthModal.tsx`)

- ✅ Новый компонент для глобальной модалки входа
- ✅ Использует `isAuthModalOpen` из `AuthContext`
- ✅ Работает на всех страницах, включая админ-панель

```typescript
export default function GlobalAuthModal() {
  const { isAuthModalOpen, authModalTab, closeAuthModal } = useAuth();
  
  return (
    <AuthModal
      isOpen={isAuthModalOpen}
      onClose={closeAuthModal}
      initialTab={authModalTab}
    />
  );
}
```

### 6. Root Layout (`/app/layout.tsx`)

- ✅ Добавлен `<GlobalAuthModal />` для работы модалки на всех страницах
- ✅ Модалка отображается внутри всех провайдеров

```typescript
<NotificationProvider>
  <TokenValidator />
  <GlobalAuthModal /> {/* 🆕 Глобальная модалка */}
  <AuthGate>
    <NavigationBurger />
    {children}
  </AuthGate>
</NotificationProvider>
```

## 🎯 Как это работает

1. Пользователь открывает `/admin/dashboard` (или любую админ-страницу)
2. `AdminLayout` проверяет наличие пользователя и его роль
3. Если пользователь не залогинен → вызывается `openAuthModal("login")`
4. `GlobalAuthModal` (в `/app/layout.tsx`) отображает модалку входа
5. Пользователь вводит email и пароль
6. `login()` отправляет запрос на бэкенд `/api/auth/login`
7. Бэкенд возвращает `{ token, user: { role, ... } }`
8. `login()` сохраняет данные в localStorage и state
9. `getRedirectUrl(user.role)` определяет URL на основе роли:
   - Если `role === "admin"` или `role === "superadmin"` → `/admin/dashboard`
   - Иначе → `/` (главная страница)
10. `login()` возвращает этот URL
11. `AuthModal` закрывается и делает `router.push(redirectUrl)`
12. Пользователь попадает на правильную страницу! 🎉

## 🔐 Роли пользователей

| Роль | Доступ | Редирект после входа |
|------|--------|----------------------|
| `student` | Обычный пользователь | `/` (главная страница) |
| `home_chef` | Обычный пользователь | `/` (главная страница) |
| `instructor` | Обычный пользователь | `/` (главная страница) |
| `admin` | Админ-панель | `/admin/dashboard` |
| `superadmin` | Админ-панель + все права | `/admin/dashboard` |

## 🧪 Тестирование

### Тест 1: Вход как обычный пользователь
1. Откройте `/`
2. Нажмите "Войти"
3. Введите `user@example.com` / `password123`
4. **Ожидается**: Редирект на `/` (главная страница, но уже залогиненный)

### Тест 2: Вход как админ
1. Откройте `/admin/dashboard`
2. Откроется модалка входа
3. Введите `admin@example.com` / `admin123`
4. **Ожидается**: Редирект на `/admin/dashboard`

### Тест 3: Вход как superadmin
1. Откройте любую страницу
2. Нажмите "Войти"
3. Введите `superadmin@example.com` / `super123`
4. **Ожидается**: Редирект на `/admin/dashboard`

## 🚀 Dev Mode

В режиме разработки (`NODE_ENV === "development"`) следующие email автоматически получают доступ к админ-панели:

- `admin@example.com`
- `dima@example.com`

Это позволяет тестировать админ-панель без настройки бэкенда.

## 📝 TODO для продакшена

- [ ] Убрать dev-mode доступ в `AdminLayout.tsx` (строки 39-46)
- [ ] Настроить роли на бэкенде (убедиться, что API возвращает правильные роли)
- [ ] Добавить middleware для защиты админ-маршрутов на уровне Next.js
- [ ] Добавить unit-тесты для `getRedirectUrl()`

## 🔗 Связанные файлы

- `/contexts/AuthContext.tsx` - Основная логика auth + глобальное состояние модалки
- `/contexts/UserContext.tsx` - Типы пользователя (добавлена роль "superadmin")
- `/components/auth/AuthModal.tsx` - UI модалки входа/регистрации
- `/components/auth/GlobalAuthModal.tsx` - 🆕 Глобальная обёртка для модалки
- `/app/layout.tsx` - 🆕 Рендерит GlobalAuthModal для всех страниц
- `/app/admin/layout.tsx` - Защита админ-панели + вызов openAuthModal()
- `/lib/admin/navigation-schema.ts` - RBAC для навигации
