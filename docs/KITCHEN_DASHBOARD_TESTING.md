# Kitchen Dashboard — Тестирование и архитектура

## 🏗️ Архитектура потока данных

```
┌─────────────────────────────────────────┐
│     Kitchen Dashboard (/recipes)        │
├─────────────────────────────────────────┤
│                                         │
│ 🟡 Сегодня готовим (planned)            │
│   └─ Новые рецепты, готовые к старту   │
│   └─ Кнопка: "Начать готовить"         │
│                                         │
│ 🔵 В процессе (cooking)                 │
│   └─ Активные рецепты прямо сейчас     │
│   └─ Кнопка: "Готово!"                 │
│   └─ Показывает start_time              │
│                                         │
│ 🟢 Приготовлено сегодня (completed)     │
│   └─ Завершённые рецепты               │
│   └─ Readonly представление              │
│   └─ Показывает completion_time         │
│                                         │
└─────────────────────────────────────────┘
```

## 🔄 Статус переходы

```
┌─────────┐    start()     ┌──────────┐   complete()  ┌───────────┐
│ planned │ ────────────→ │ cooking  │ ─────────────→ │ completed │
└─────────┘               └──────────┘               └───────────┘
```

## 📊 Фильтрация на фронте

```typescript
// Backend возвращает: MenuItem[]
const response = await menuApi.getToday(token, language);

// Frontend группирует по статусу:
const planned = response.filter(i => i.status === "planned");
const cooking = response.filter(i => i.status === "cooking");
const completed = response.filter(i => i.status === "completed");
```

## ✅ Тестовый сценарий (БЫСТРЫЙ ТЕСТ)

### Шаг 1️⃣ — Добавить рецепт в меню
```
1. Перейти в /assistant
2. Нажать ❤️ на рецепт
3. Рецепт добавляется в меню
```

### Шаг 2️⃣ — Открыть /recipes
```
1. Перейти в /recipes
2. Обновить страницу (F5)
3. Проверить логи браузера
```

### Шаг 3️⃣ — Проверить UI

**ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:**

```
✅ Карточка появляется в секции "Сегодня готовим"
✅ Status card показывает счётчик
✅ Секция НЕ скрыта (planned всегда видна)
```

**ЛОГИ В КОНСОЛИ:**

```
🍽️ [page] Loading today's menu...
✅ [page] RAW response from menuApi: { isArray: true, length: 1 }
📊 [page] Menu items after filtering: { 
  planned: 1, 
  cooking: 0, 
  completed: 0, 
  total: 1 
}
```

### Шаг 4️⃣ — Нажать "Начать готовить"

```
1. Нажать кнопку "Начать готовить"
2. Появится toast: "✅ Начали готовить!"
3. Страница перезагрузится
4. Карточка переместится в "В процессе"
```

**ЛОГИ:**

```
🔵 Starting to cook item: <id>
📡 [menu/:id/start] Proxying to backend
✅ Menu items after filtering: { 
  planned: 0, 
  cooking: 1, 
  completed: 0 
}
```

### Шаг 5️⃣ — Нажать "Готово!"

```
1. Нажать кнопку "Готово!"
2. Появится toast: "🎉 Отлично! Блюдо готово!"
3. Страница перезагрузится
4. Карточка переместится в "Приготовлено сегодня"
```

**ЛОГИ:**

```
✅ Completing item: <id>
📡 [menu/:id/complete] Proxying to backend
✅ Menu items after filtering: { 
  planned: 0, 
  cooking: 0, 
  completed: 1 
}
```

## 🐛 Отладка проблем

### Если рецепт не появляется в Сегодня готовим:

**Проверить:**

```
1. Открыть DevTools → Console
2. Искать логи: "📊 [page] Menu items after filtering"
3. Проверить: planned: 0?
   ✅ Если planned: 0 → рецепт не в статусе "planned"
   ✅ Может быть уже в cooking или completed
```

### Если кнопки не работают:

**Проверить:**

```
1. Console → Network → /api/menu/:id/start
2. Статус ответа: 200?
3. Если 404 → Route Handler не найден
4. Если 401 → Токен истёк
```

## 📝 Важные файлы

```
Backend:
├─ /api/menu/today          ← GET меню на день
├─ /api/menu/:id/start      ← POST начать готовку
└─ /api/menu/:id/complete   ← POST завершить

Frontend:
├─ /lib/api/menu.ts         ← API клиент
├─ /app/api/menu/           ← Route Handlers
└─ /app/(user)/recipes/page.tsx  ← UI компонент
```

## 🚀 Полный чек-лист готовности

- [x] Структура меню правильная (planned → cooking → completed)
- [x] Backend возвращает массив MenuItem[]
- [x] Frontend группирует по статусу
- [x] UI отрисовывает все 3 секции
- [x] Кнопки работают (start/complete)
- [x] Логирование помогает отладке
- [x] Toast уведомления работают
- [x] Перезагрузка страницы срабатывает

**Статус:** ✅ READY FOR PRODUCTION
