# ✅ Локализация Assistant - Quick Checklist

## 🎯 Что исправлено

1. ✅ Убран хардкод PL → динамическая локализация (PL/RU/EN)
2. ✅ Убран frontend AI text → backend передаёт `ai.title` и `ai.reason`

---

## 🧪 Быстрая проверка

### 1. Запустить
```bash
npm run dev
```

### 2. Открыть `/assistant`

### 3. Переключить язык

| Язык | Ожидаемый заголовок |
|------|-------------------|
| 🇵🇱 PL | "AI Asystent Kuchenny" |
| 🇷🇺 RU | "AI Кухонный ассистент" |
| 🇬🇧 EN | "AI Kitchen Assistant" |

### 4. Проверить AI текст

✅ **Должно быть**:
```
[Blue box with Sparkles icon]
"Идеальное блюдо для сегодня!"
"У вас есть все необходимые ингредиенты..."
💡 Tip (если есть)
```

❌ **НЕ должно быть**:
```
"masz 0 składników w lodówce (100% pokrycia)"
"Ten przepis został zaproponowany, ponieważ..."
```

---

## 📊 Backend Response

Проверить Network → `/api/ai-recipe/recommendation`:

```json
{
  "data": {
    "recipe": { ... },
    "ai": {
      "title": "...",          // ✅ Backend генерирует
      "reason": "...",          // ✅ Backend генерирует
      "ingredientsUsed": [...], // ✅ Backend выбирает
      "tip": "..."              // ✅ Backend советует
    }
  }
}
```

---

## ✅ Если всё работает

- ✅ Заголовки переключаются по языку
- ✅ AI текст от backend (не генерируется frontend)
- ✅ Нет "masz X składników" текстов
- ✅ AIRecommendationCard показывает только ингредиенты

---

## ❌ Если не работает

### Проблема: Тексты не переключаются
**Решение**: Проверить что словари подключены в `lib/i18n/dictionaries/{lang}.ts`

### Проблема: "masz 0 składników..." всё ещё показывается
**Решение**: Проверить что секция "DLACZEGO TEN PRZEPIS?" удалена из `AIRecommendationCard.tsx`

### Проблема: TypeScript ошибки
**Решение**: Проверить что `assistant` добавлен в `lib/i18n/types.ts`

---

**Статус**: ✅ Готово к тестированию
