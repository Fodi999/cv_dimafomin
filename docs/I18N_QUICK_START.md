# i18n Quick Start Guide

## 🚀 Быстрый старт за 2 минуты

### 1. Использование переводов

```tsx
import { useLanguage } from '@/contexts/LanguageContext';

function MyComponent() {
  const { t, language } = useLanguage();
  
  return (
    <div>
      <h1>{t.academy.hero.title}</h1>
      <p>{t.common.description}</p>
      <button>{t.common.save}</button>
    </div>
  );
}
```

### 2. Добавление новых переводов

**а) Добавить в Polish** (reference language):
```ts
// i18n/pl/common.ts
export const common = {
  myNewKey: "Mój nowy klucz",
} as const;
```

**б) Добавить в English**:
```ts
// i18n/en/common.ts
export const common = {
  myNewKey: "My new key",
} as const;
```

**в) Добавить в Russian**:
```ts
// i18n/ru/common.ts
export const common = {
  myNewKey: "Мой новый ключ",
} as const;
```

**г) Проверить**:
```bash
npm run check:i18n
```

### 3. Проверка покрытия

```bash
# Одноразовая проверка
npm run check:i18n

# Watch mode (автоперезапуск)
npm run check:i18n:watch
```

## ✅ Что проверяет система

- ✅ Все языки имеют одинаковую структуру
- ✅ Нет пропущенных переводов
- ✅ Нет пустых значений
- ✅ AI-generated переводы помечены для ревью
- ✅ TypeScript типизация работает

## 🔥 Фишки

### Autocomplete из коробки
```tsx
t.academy.hero.  // 👈 IDE покажет все доступные ключи
```

### Dev-time warnings
В development mode автоматически показываются предупреждения:
- 🤖 AI-generated переводы
- 🚫 Пустые значения
- ⚠️ Пропущенные ключи

### Type-safe
```tsx
t.academy.hero.title  // ✅ OK
t.academy.hero.wrong  // ❌ TypeScript error
```

## 📖 Полная документация

Смотри [I18N_SYSTEM.md](./I18N_SYSTEM.md) для:
- Детальная архитектура
- AI-translations layer
- CI/CD интеграция
- Best practices
- Troubleshooting

## 🎯 Чек-лист перед коммитом

```bash
# 1. Проверить покрытие
npm run check:i18n

# 2. Проверить типы
npx tsc --noEmit

# 3. Запустить в браузере
npm run dev
# Проверить, что нет ⚠️ в правом нижнем углу
```

## 🆘 Помощь

**Проблема**: TypeScript не видит новый ключ
**Решение**: Перезапустить TS Server (Cmd+Shift+P → "TypeScript: Restart TS Server")

**Проблема**: Coverage check fails
**Решение**: Запустить `npm run check:i18n` и исправить показанные ошибки

**Проблема**: AI-generated warning
**Решение**: Проверить перевод с native speaker и убрать маркер `[AI]`

---

Готово! 🎉 Теперь можно работать с переводами.
