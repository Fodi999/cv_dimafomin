# Technical Debt & Refactoring Opportunities

> Документ для отслеживания технического долга и возможностей для улучшения кодовой базы

**Последнее обновление**: 28 декабря 2025  
**Статус**: 🟡 В процессе

---

## 🎯 Приоритеты

| Приоритет | Описание |
|-----------|----------|
| 🔴 **Critical** | Блокирует разработку, нужно исправить немедленно |
| 🟠 **High** | Важно, но не блокирует. Исправить в ближайшие спринты |
| 🟡 **Medium** | Желательно исправить, улучшает качество кода |
| 🟢 **Low** | Nice-to-have, можно делать постепенно |

---

## 📊 Текущий статус

- **Critical**: 0 ✅
- **High**: 0 ✅
- **Medium**: 2 🟡
- **Low**: 1 🟢

---

## 1️⃣ Унификация RecipeCard компонентов

**Приоритет**: 🟡 **Medium**  
**Категория**: Code Quality / DRY  
**Дата**: 28.12.2025

### Проблема

В проекте существует **3 разных компонента** с одинаковым названием:

```
components/
├── assistant/RecipeCard.tsx      # Для AI-рекомендаций
├── market/RecipeCard.tsx         # Для маркетплейса
└── recipes/RecipeCard.tsx        # Для списка рецептов
```

### Анализ различий

Нужно провести анализ чтобы понять:

1. **Какие пропсы принимают?**
   ```bash
   # Проверить интерфейсы
   grep -A 10 "interface.*RecipeCard" components/*/RecipeCard.tsx
   ```

2. **Какую логику содержат?**
   - assistant/RecipeCard - возможно показывает AI-инсайты, калории
   - market/RecipeCard - показывает цену, кнопку покупки
   - recipes/RecipeCard - базовая карточка с кнопкой "открыть"

3. **Можно ли использовать общий базовый компонент?**

### Варианты решения

#### Вариант A: Зафиксировать различия (быстро)
```
docs/
└── COMPONENTS_REGISTRY.md
    ├── RecipeCard (assistant) - для AI recommendations
    ├── RecipeCard (market) - для marketplace с ценами
    └── RecipeCard (recipes) - базовая карточка
```

**Плюсы**: 
- Быстро (5 минут)
- Документирует текущее состояние

**Минусы**: 
- Не решает дублирование кода

#### Вариант B: Base component + variants (правильно)
```tsx
// components/recipes/BaseRecipeCard.tsx
interface BaseRecipeCardProps {
  recipe: Recipe;
  variant?: 'default' | 'market' | 'ai-recommendation';
  showPrice?: boolean;
  showAIInsights?: boolean;
  showCalories?: boolean;
  onPurchase?: () => void;
  className?: string;
}

// Использование:
<BaseRecipeCard 
  recipe={recipe} 
  variant="market" 
  showPrice 
/>

<BaseRecipeCard 
  recipe={recipe} 
  variant="ai-recommendation" 
  showCalories 
  showAIInsights 
/>
```

**Плюсы**:
- DRY принцип
- Единая логика
- Проще поддерживать

**Минусы**:
- Требует времени (~2-3 часа)
- Нужно тестировать все варианты использования

#### Вариант C: Композиция (гибко)
```tsx
// components/recipes/RecipeCard/
├── index.tsx              # Базовая карточка
├── RecipeCardHeader.tsx   # Переиспользуемый header
├── RecipeCardBody.tsx     # Контент
├── RecipeCardFooter.tsx   # Actions
├── RecipeCardPrice.tsx    # Для market
└── RecipeCardAIBadge.tsx  # Для AI

// Использование:
<RecipeCard>
  <RecipeCardHeader {...} />
  <RecipeCardBody {...} />
  <RecipeCardPrice price={10} />  {/* Только для market */}
  <RecipeCardFooter {...} />
</RecipeCard>
```

**Плюсы**:
- Максимальная гибкость
- Легко расширять

**Минусы**:
- Более сложная структура

### Рекомендация

**Фаза 1** (сейчас): Вариант A - зафиксировать в документации  
**Фаза 2** (когда будет время): Вариант B или C

### Action Items

- [x] Проанализировать пропсы всех трёх RecipeCard
- [x] Создать `docs/RECIPE_CARD_ANALYSIS.md` с описанием различий
- [x] Добавить JSDoc комментарии в каждый компонент
- [ ] Извлечь общие UI элементы (RecipeDifficultyBadge, RecipeCookingTime)
- [ ] При рефакторинге - выбрать вариант B или C

### Команды для анализа

```bash
# Посмотреть интерфейсы
grep -A 20 "interface.*RecipeCard\|type.*RecipeCard" components/*/RecipeCard.tsx

# Посмотреть импорты (зависимости)
grep "^import" components/*/RecipeCard.tsx

# Посмотреть размер файлов
wc -l components/*/RecipeCard.tsx

# Найти все использования
grep -r "import.*RecipeCard" --include="*.tsx" --include="*.ts"
```

---

## 2️⃣ Legacy i18n файлы

**Приоритет**: 🟡 **Medium**  
**Категория**: Code Cleanup  
**Дата**: 28.12.2025

### Статус

**✅ Phase 1 завершён** (28.12.2025):
- ✅ `lib/i18n-dictionary.ts` - УДАЛЁН
- ✅ `lib/translations.ts` - УДАЛЁН
- ✅ `components/LanguageSwitcher.tsx` - мигрирован

**🟡 Phase 2 осталось**:
- ⏳ `lib/profile-translations.ts` (1 использование)
- ⏳ `hooks/useTranslations.ts` (2 использования)
- ⏳ `hooks/useProfileTranslations.ts` (1 использование)

### Документация

Полный план миграции: **`docs/I18N_DEPRECATION_PLAN.md`**

### Прогресс

```
████████░░░░░░  40% (2/5 files)
```

### Следующие шаги

1. **Мигрировать компонент `AIActions.tsx`**:
   ```tsx
   // Было:
   import { useTranslations } from "@/hooks/useTranslations";
   
   // Стало:
   import { useLanguage } from "@/contexts/LanguageContext";
   const { t } = useLanguage();
   // t.common.save вместо t("common.save")
   ```

2. **Мигрировать `CoreSettingsSection.tsx`** (аналогично)

3. **Мигрировать `app/profile/[id]/page.tsx`**:
   ```tsx
   // Было:
   import { useProfileTranslations } from "@/hooks/useProfileTranslations";
   
   // Стало:
   import { useLanguage } from "@/contexts/LanguageContext";
   const { t } = useLanguage();
   // t.profile.stats.recipes вместо t("profile.stats.recipes")
   ```

4. **Удалить legacy файлы**:
   ```bash
   rm lib/profile-translations.ts
   rm hooks/useTranslations.ts
   rm hooks/useProfileTranslations.ts
   ```

### Action Items

- [x] Создать план миграции
- [x] Удалить неиспользуемые файлы (phase 1)
- [ ] Мигрировать AIActions.tsx
- [ ] Мигрировать CoreSettingsSection.tsx
- [ ] Мигрировать profile/[id]/page.tsx
- [ ] Удалить оставшиеся legacy файлы
- [ ] Обновить команду onboarding

---

## 3️⃣ Дублирование API клиентов

**Приоритет**: 🟢 **Low**  
**Категория**: Code Quality  
**Дата**: 28.12.2025

### Проблема

Существует несколько мест с API клиентами:

```
lib/
├── api/                  # Новая структурированная API
│   ├── base.ts
│   ├── auth.ts
│   ├── fridge.ts
│   └── ...
├── api.ts                # Старый API клиент (legacy)
└── auth-interceptor.ts   # Отдельная логика

src/
├── api/
│   └── client.ts         # Ещё один клиент
└── utils/
    └── api.ts            # И ещё один
```

### Анализ

Нужно проверить:
1. Какие файлы активно используются?
2. Можно ли объединить в один?
3. Есть ли дублирование логики?

### Рекомендация

Когда будет время:
1. Провести аудит использования
2. Выбрать один canonical клиент
3. Постепенно мигрировать

---

## 🛠️ Инструменты для мониторинга

### Найти дублирующийся код
```bash
# Найти похожие файлы
npx jscpd --min-lines 10 --min-tokens 50 components/
```

### Найти неиспользуемые экспорты
```bash
# С помощью ts-prune
npx ts-prune
```

### Проверить размер бандла
```bash
npm run build
npx @next/bundle-analyzer
```

### Найти TODO/FIXME комментарии
```bash
grep -r "TODO\|FIXME\|HACK\|XXX" --include="*.ts" --include="*.tsx" | wc -l
```

---

## 📈 Метрики качества кода

| Метрика | Текущее | Цель | Статус |
|---------|---------|------|--------|
| TypeScript ошибок | 0 | 0 | ✅ |
| Дублирующихся RecipeCard | 3 | 1 | 🟡 |
| Legacy i18n файлов | 3 | 0 | 🟡 |
| Coverage i18n | 100% | 100% | ✅ |
| Размер bundle (гл. страница) | ? | <500KB | ❓ |

---

## 🎯 План на 2025

### Q1 2025
- [ ] Завершить i18n cleanup (Phase 2)
- [ ] Провести аудит RecipeCard компонентов
- [ ] Документировать различия в COMPONENTS_REGISTRY.md

### Q2 2025
- [ ] Унифицировать RecipeCard (если целесообразно)
- [ ] Провести аудит API клиентов
- [ ] Настроить автоматический мониторинг bundle size

### Q3-Q4 2025
- [ ] Refactoring API clients
- [ ] Performance optimization
- [ ] Bundle size optimization

---

## 📚 Связанные документы

- [I18N_DEPRECATION_PLAN.md](./I18N_DEPRECATION_PLAN.md) - План миграции i18n
- [I18N_SYSTEM.md](./I18N_SYSTEM.md) - Документация новой системы
- [ARCHITECTURE_STATUS.md](../ARCHITECTURE_STATUS.md) - Общая архитектура

---

## 💡 Как добавить новый технический долг?

1. Добавить секцию с приоритетом
2. Описать проблему
3. Предложить варианты решения
4. Создать action items
5. Обновить метрики

---

*Этот документ — living document. Обновляйте его по мере работы над проектом.*
