# Fix: Категории ингредиентов (category vs nutritionGroup)

## Дата: 7 января 2026

## 🐛 Проблема

Фронтенд показывал **nutritionGroup** (белок, углеводы, жиры) вместо **category** (рыба, мясо, овощи).

### Пример ошибки:
```
Лосось
protein  ❌ (показывалось нутриентная группа)
г
```

### Должно быть:
```
Лосось
🐟 Рыба  ✅ (кулинарная категория)
💪 Белок (дополнительно)
г
```

---

## ✅ Решение

### 1. Создан единый источник правды

**Файл:** `/lib/constants/ingredientCategories.ts`

```typescript
// Кулинарные категории (для UI)
export const CATEGORY_LABELS: Record<string, string> = {
  fish: "Рыба",
  meat: "Мясо",
  egg: "Яйца",
  vegetable: "Овощи",
  fruit: "Фрукты",
  // ... 22 категории
};

// Нутриентные группы (дополнительная информация)
export const NUTRITION_LABELS: Record<string, string> = {
  protein: "Белок",
  carbohydrate: "Углеводы",
  fat: "Жиры",
  // ... 7 групп
};

// Хелперы
getCategoryLabel(category: string): string
getNutritionLabel(nutritionGroup: string): string
getCategoryIcon(category: string): string
getNutritionIcon(nutritionGroup: string): string
```

---

### 2. Обновлен AddIngredientDialog

**Файл:** `/components/admin/catalog/ingredients/AddIngredientDialog.tsx`

#### Изменения:
- ❌ Удалено: `getCategoryNameRu()` - старая функция с неполным маппингом
- ✅ Добавлено: Импорт хелперов из `ingredientCategories.ts`
- ✅ Показываются **оба поля**:
  - **category** (основное) - кулинарная категория с иконкой
  - **nutritionGroup** (дополнительное) - нутриентная группа с иконкой

#### Пример UI:
```tsx
{/* Кулинарная категория (ОСНОВНАЯ) */}
<div className="flex items-center gap-1.5">
  <span>🐟</span>
  <span className="bg-blue-100">Рыба</span>
</div>

{/* Нутриентная группа (ДОПОЛНИТЕЛЬНАЯ) */}
{ingredient.nutritionGroup && (
  <div className="flex items-center gap-1.5 text-xs text-muted">
    <span>💪</span>
    <span className="bg-purple-100">Белок</span>
  </div>
)}
```

---

### 3. Обновлены типы

#### `/lib/types.ts`
```typescript
export interface CatalogIngredient {
  // ... existing fields
  category: string; // Кулинарная категория (fish, meat, vegetable, etc.)
  nutritionGroup?: string; // Нутриентная группа (protein, carbohydrate, fat, etc.)
  // ... other fields
}

export interface FridgeItem {
  ingredient: {
    // ... existing fields
    category: string;
    nutritionGroup?: string;
  }
}
```

#### `/hooks/useIngredients.ts`
```typescript
export interface Ingredient {
  // ... existing fields
  category: string; // Кулинарная категория
  nutritionGroup?: string; // Нутриентная группа
}
```

---

## 📊 Архитектура (как должно быть)

| Поле | Смысл | Где показывать | Пример значений |
|------|-------|----------------|-----------------|
| **category** | Кулинарная категория | UI, таблицы, модалки | fish, meat, vegetable, fruit |
| **nutritionGroup** | Нутриентная роль | Бейджи, тултипы, аналитика | protein, carbohydrate, fat |

---

## 🎯 Что показывается пользователю

### Модалка "Продукт уже существует"
```
Лосось
🐟 Рыба        ← category (ОСНОВНОЕ)
💪 Белок       ← nutritionGroup (доп. инфо)
г              ← unit
```

### Таблица каталога
- ✅ Показывается **только category** (кулинарная)
- ❌ **НЕ показывается** nutritionGroup

**Почему:** Админ думает кулинарно, не нутриентно. Нутриенты - вторичный слой данных.

---

## 🧪 Тестирование

### Проверить:
1. Открыть админ-панель → Каталог → Ингредиенты
2. Нажать "Добавить продукт"
3. Ввести "Лосось" или "Salmon"
4. Система покажет: "Такой продукт уже существует"
5. Должно отображаться:
   - ✅ Название: Лосось
   - ✅ Категория: 🐟 Рыба (синий бейдж)
   - ✅ Нутриент: 💪 Белок (фиолетовый бейдж)
   - ✅ Единица: г (серый бейдж)

---

## 🚀 Итог

**До:**
- ❌ Показывался `protein` вместо `fish`
- ❌ Пользователь не понимал какой это продукт
- ❌ Нет единого источника маппинга

**После:**
- ✅ Показывается `fish` (Рыба) как основная категория
- ✅ Показывается `protein` (Белок) как дополнительная информация
- ✅ Единый источник правды в `ingredientCategories.ts`
- ✅ Иконки для визуального различия
- ✅ Правильные цвета бейджей (синий = категория, фиолетовый = нутриент)

---

## 📝 Для разработчиков

### Правило:
- **category** → UI (таблицы, карточки, модалки)
- **nutritionGroup** → Analytics, tooltips, дополнительная информация

### Всегда использовать:
```typescript
import { getCategoryLabel, getNutritionLabel } from '@/lib/constants/ingredientCategories';

// ✅ Правильно
<span>{getCategoryLabel(ingredient.category)}</span>
<span className="text-xs">{getNutritionLabel(ingredient.nutritionGroup)}</span>

// ❌ Неправильно
<span>{ingredient.nutritionGroup}</span> // Показываем нутриент вместо категории!
```
