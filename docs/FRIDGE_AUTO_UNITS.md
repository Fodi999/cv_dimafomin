# 🔧 Автозаполнение единиц измерения в холодильнике

## ✅ Текущая реализация

### Компоненты
- **`/components/fridge/FridgeForm.tsx`** - форма добавления продукта
- **`/components/fridge/IngredientAutocomplete.tsx`** - поиск ингредиентов
- **`/lib/api/fridge.ts`** - API для работы с холодильником
- **`/lib/types.ts`** - типы данных

### Принцип работы

1. **Пользователь вводит** название продукта в autocomplete
2. **API возвращает** список ингредиентов с полем `unit`:
   ```typescript
   {
     id: "abc-123",
     name: "Молоко 2%",
     unit: "ml",  // 👈 Единица измерения из БД
     category: "dairy",
     defaultShelfLifeDays: 7
   }
   ```

3. **При выборе** продукта:
   - `unit` автоматически устанавливается в форму
   - Отображается в UI: "Количество (ml)"
   - Показывается в информационном блоке

4. **При сохранении**:
   ```typescript
   {
     ingredientId: "abc-123",
     quantity: 500,
     unit: "ml",  // 👈 Из autocomplete
     expiresAt: "2026-01-27T00:00:00Z",
     priceInput: {
       value: 50,
       per: "l"
     }
   }
   ```

## 🎨 UI элементы

### 1. Информационный блок (после выбора продукта)
```tsx
<div className="p-4 bg-gradient-to-r from-sky-50 to-cyan-50">
  <p>
    Выбранный продукт: <strong>Молоко 2%</strong>
  </p>
  <p>
    Единица: <span className="font-medium">ml</span> • 
    Категория: Молочные
  </p>
  <p>
    Срок годности: 27 января 2026 г. (7 дней)
  </p>
</div>
```

### 2. Поле количества
```tsx
<label>
  Количество (ml)  {/* 👈 Единица в label */}
</label>
<input
  type="number"
  placeholder="500 ml"  {/* 👈 Единица в placeholder */}
  disabled={!selectedIngredient}
/>
```

### 3. Autocomplete suggestions
```tsx
<li>
  Молоко 2% <span className="badge">ml</span>  {/* 👈 Badge с единицей */}
</li>
```

## 📊 Единицы измерения в БД

| Единица | Количество | Примеры |
|---------|-----------|---------|
| g       | 102       | Мука, сахар, соль |
| ml      | 58        | Молоко, масло, вода |
| pcs     | 32        | Яйца, картофель |
| tsp     | 12        | Специи |
| tbsp    | 6         | Масло, соусы |
| cup     | 3         | Мука |
| pinch   | 1         | Соль |

## 🔒 Правила

- ❌ **НЕ давать пользователю выбирать единицу** - она фиксированная в БД
- ✅ **Всегда использовать unit из API** - `selectedIngredient.unit`
- ✅ **Показывать единицу в UI** - в label, placeholder, badge
- ✅ **Disabled/read-only** - пользователь не может изменить

## 🌍 Локализация единиц

В файлах `/i18n/{lang}/fridge.ts`:

```typescript
units: {
  g: "граммы (г)",
  kg: "килограммы (кг)",
  ml: "миллилитры (мл)",
  l: "литры (л)",
  pcs: "штуки (шт)",
  tsp: "чайные ложки (ч.л.)",
  tbsp: "столовые ложки (ст.л.)",
  cup: "стаканы",
  pinch: "щепотки"
}
```

Использование:
```tsx
{t?.fridge?.units?.[selectedIngredient.unit] || selectedIngredient.unit}
```

## ✅ Проверочный чек-лист

- [x] API возвращает `unit` в autocomplete
- [x] `unit` автоматически устанавливается при выборе продукта
- [x] `unit` отображается в UI (label + placeholder)
- [x] `unit` НЕ редактируется пользователем
- [x] `unit` передается в API при сохранении
- [x] Срок годности автоматически рассчитывается
- [x] Цена опциональна (recommended)
- [x] Локализация названий продуктов

## 🎯 Преимущества

1. **Консистентность данных** - единица всегда соответствует продукту
2. **Простота UX** - на 1 поле меньше для заполнения
3. **Избегание ошибок** - нельзя указать молоко в граммах
4. **Автоматические расчёты** - система знает, как конвертировать единицы
5. **Интеграция с рецептами** - единицы совпадают между холодильником и рецептами

---

**Статус:** ✅ Полностью реализовано и работает  
**Дата:** 2026-01-20  
**Версия:** 1.0
