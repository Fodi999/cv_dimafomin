# 🏗️ Architecture: Role-Based Routing

## 📋 Обзор

Проект теперь использует **3-зонную архитектуру** с разделением по ролям:

```
🔓 Публичная зона (/)      →  Гости (не авторизованы)
👤 Пользовательская (/app) →  Обычные пользователи
🛡️ Админ-панель (/admin)    →  Администраторы
```

## 🎯 Принцип работы

**Роль + состояние авторизации = стартовая точка**

### 1. 🔓 Гость (не авторизован)

- **Доступ**: только публичная зона `/`
- **Страницы**: Главная, Академия (превью), Цены, О нас, Войти/Регистрация
- **Редирект**: При попытке зайти в `/app` или `/admin` → `/`

**Что видит:**
```
✅ /             Главная
✅ /academy      Академия (превью)
✅ /pricing      Цены
✅ /about        О нас
❌ /app          → Редирект на /
❌ /admin        → Редирект на /
```

### 2. 👤 Обычный пользователь (USER)

- **Доступ**: `/` (публичная) + `/app` (приложение)
- **Страницы**: Холодильник, Рецепты, AI Ассистент, Академия, Токены
- **Редирект после входа**: `/app`
- **Защита**: При попытке зайти в `/admin` → `/app`

**Что видит:**
```
✅ /             Главная (лендинг)
✅ /app          Dashboard пользователя
✅ /app/fridge   Холодильник
✅ /app/recipes  Рецепты
✅ /app/assistant AI Ассистент
✅ /app/academy  Академия
✅ /app/tokens   Токены
❌ /admin        → Редирект на /app
```

### 3. 🛡️ Администратор (ADMIN / SUPERADMIN)

- **Доступ**: `/` (публичная) + `/admin` (админ-панель)
- **Страницы**: Dashboard, Користувачі, Рецепти, Активність, Налаштування
- **Редирект после входа**: `/admin/dashboard`
- **Защита**: При попытке зайти в `/app` → `/admin`

**Что видит:**
```
✅ /             Главная (лендинг)
✅ /admin        Admin Dashboard
✅ /admin/users  Користувачі
✅ /admin/recipes Рецепти
✅ /admin/activity Активність
✅ /admin/settings Налаштування
❌ /app          → Редирект на /admin
```

## 🔧 Технические детали

### Middleware (`/middleware.ts`)

**Задачи:**
1. i18n (язык из cookies)
2. **Role-based routing** (защита маршрутов)

**Логика:**

```typescript
// Гость
if (!token) {
  if (pathname.startsWith("/app") || pathname.startsWith("/admin")) {
    redirect("/"); // Гость не может в app/admin
  }
}

// Админ
if (role === "admin" || role === "superadmin") {
  if (pathname.startsWith("/app")) {
    redirect("/admin"); // Админ не может в user app
  }
}

// Пользователь
if (token && role !== "admin") {
  if (pathname.startsWith("/admin")) {
    redirect("/app"); // User не может в admin
  }
}
```

### AuthContext (`/contexts/AuthContext.tsx`)

**Изменения:**

1. **Сохранение в cookies**:
   ```typescript
   document.cookie = `token=${token}; path=/; max-age=${7 * 24 * 60 * 60}`;
   document.cookie = `role=${user.role}; path=/; max-age=${7 * 24 * 60 * 60}`;
   ```

2. **Редирект по роли**:
   ```typescript
   const getRedirectUrl = (userRole: string): string => {
     if (userRole === "admin" || userRole === "superadmin") {
       return "/admin/dashboard"; // Админ → админ-панель
     }
     return "/app"; // User → приложение
   };
   ```

3. **Logout**:
   ```typescript
   document.cookie = "token=; path=/; max-age=0"; // Удаляем cookies
   document.cookie = "role=; path=/; max-age=0";
   ```

## 📁 Структура файлов

```
app/
├── layout.tsx          # Корневой layout (все провайдеры)
├── page.tsx            # 🔓 Публичная главная (лендинг)
│
├── app/                # 👤 Пользовательское приложение
│   ├── layout.tsx      # UserLayout (меню пользователя)
│   ├── page.tsx        # User Dashboard
│   ├── fridge/
│   ├── recipes/
│   ├── assistant/
│   ├── academy/
│   └── tokens/
│
├── admin/              # 🛡️ Админ-панель
│   ├── layout.tsx      # AdminLayout (AdminSidebar)
│   ├── page.tsx        # Admin Dashboard
│   ├── users/
│   ├── recipes/
│   ├── activity/
│   └── settings/
│
├── academy/            # 🔓 Публичная академия (превью)
├── pricing/            # 🔓 Цены
└── about/              # 🔓 О нас
```

## 🎨 Навигация по зонам

### 🔓 Публичное меню (Landing Navigation)

```
Главная      /
Як це працює /how-it-works
Академія     /academy
Ціни         /pricing
Увійти       → AuthModal
```

❌ **Нет**: Холодильник, AI, Токени, Админка

### 👤 Пользовательское меню (User Navigation)

```
📍 Кухня
   ├ Холодильник   /app/fridge
   ├ Рецепти       /app/recipes
   ├ AI Асистент   /app/assistant
   └ Мої рецепти   /app/recipes/saved

📚 Розвиток
   └ Академія      /app/academy

💰 Економіка
   └ Токени        /app/tokens
```

❌ **Нет**: Адміністрація, Користувачі, Налаштування

### 🛡️ Админское меню (Admin Sidebar)

```
📊 Dashboard       /admin
👥 Користувачі    /admin/users
📖 Рецепти         /admin/recipes
📈 Активність      /admin/activity
⚙️ Налаштування    /admin/settings
```

❌ **Нет**: Холодильник, AI Асистент, Токены

## 🧪 Тестирование

### Тест 1: Гость пытается зайти в `/app`
```bash
1. Открыть /app/fridge
2. Ожидается: Редирект на /
3. Middleware log: 🚫 Guest tried to access: /app/fridge → redirecting to /
```

### Тест 2: User пытается зайти в `/admin`
```bash
1. Войти как user@example.com
2. Открыть /admin/users
3. Ожидается: Редирект на /app
4. Middleware log: 👤 User tried to access /admin → redirecting to /app
```

### Тест 3: Admin пытается зайти в `/app`
```bash
1. Войти как admin@example.com
2. Открыть /app/fridge
3. Ожидается: Редирект на /admin
4. Middleware log: 🛡 Admin tried to access /app → redirecting to /admin
```

### Тест 4: Редирект после логина
```bash
# User
1. Открыть /, нажать "Войти"
2. Ввести user@example.com
3. Ожидается: Редирект на /app
4. AuthContext log: 👤 Regular user, redirecting to /app

# Admin
1. Открыть /, нажать "Войти"
2. Ввести admin@example.com
3. Ожидается: Редирект на /admin/dashboard
4. AuthContext log: 🔐 Admin detected, redirecting to /admin/dashboard
```

## 📝 TODO (следующие шаги)

- [ ] Создать `/app/layout.tsx` (UserLayout)
- [ ] Переместить страницы: `/fridge` → `/app/fridge`, `/recipes` → `/app/recipes`
- [ ] Создать LandingNavigation (публичное меню)
- [ ] Создать UserNavigation (меню для /app)
- [ ] Обновить AdminSidebar (убрать user-пункты)

## 🚀 Результат

**До:**
- ❌ Одно меню для всех ролей
- ❌ Гости видят "Холодильник", "AI"
- ❌ Users видят "Адміністрація"
- ❌ Admins видят "Токены", "Холодильник"
- ❌ Никакой защиты на уровне middleware

**После:**
- ✅ 3 отдельных меню (Guest, User, Admin)
- ✅ Middleware защищает маршруты по роли
- ✅ Правильный редирект после login
- ✅ Каждый видит только своё
- ✅ Профессиональная архитектура

**Статус:** 🟢 MVP → Продукт (архитектура готова!)
