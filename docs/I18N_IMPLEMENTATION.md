# i18n System Implementation Guide

## ✅ Що вже зроблено

### 1. Встановлено next-intl
```bash
npm install next-intl
```

### 2. Створено структуру перекладів
```
/locales
  /pl/common.json  ✅
  /en/common.json  ✅
  /ru/common.json  ✅
```

### 3. Створено конфігурацію
- `/i18n.ts` - конфігурація next-intl ✅
- `/middleware.ts` - роутинг локалей ✅
- `next.config.ts` - інтеграція next-intl ✅

### 4. Оновлено LanguageContext
- Інтеграція з Settings API ✅
- Автоматична синхронізація з профілем ✅
- Fallback до browser language ✅

## 🔄 Наступні кроки (опціонально)

### Варіант А: Повна міграція на next-intl (складно)

**Потребує:**
1. Перемістити всі файли з `app/*` до `app/[locale]/*`
2. Оновити всі import шляхи
3. Оновити всі посилання в компонентах
4. Тестування всіх маршрутів

**Переваги:**
- Повна підтримка SSR з перекладами
- SEO оптимізація для кожної мови
- Автоматичний роутинг `/pl/...`, `/en/...`, `/ru/...`

**Недоліки:**
- Великий рефакторинг (100+ файлів)
- Ризик зламати існуючий функціонал
- 2-3 години роботи

### Варіант Б: Гібридний підхід (рекомендовано)

**Що залишається:**
- Поточна структура `app/*` без змін
- LanguageContext працює як є
- Settings API зберігає мову користувача

**Що додається:**
- `useTranslations()` hook з next-intl
- JSON-файли перекладів замість `lib/translations.ts`
- Динамічна зміна мови без перезавантаження

**Імплементація:**

1. **Створити простий хук для перекладів:**
```typescript
// lib/use-translations.ts
import { useLanguage } from '@/contexts/LanguageContext';
import pl from '@/locales/pl/common.json';
import en from '@/locales/en/common.json';
import ru from '@/locales/ru/common.json';

const messages = { pl, en, ru };

export function useTranslations() {
  const { language } = useLanguage();
  return {
    t: (key: string) => {
      const keys = key.split('.');
      let value: any = messages[language];
      for (const k of keys) {
        value = value?.[k];
      }
      return value || key;
    },
    language,
  };
}
```

2. **Використання в компонентах:**
```tsx
// Before
import { useLanguage } from '@/contexts/LanguageContext';
const { t } = useLanguage();

// After
import { useTranslations } from '@/lib/use-translations';
const { t } = useTranslations();

// Usage
<h1>{t('settings.title')}</h1>
<p>{t('settings.subtitle')}</p>
```

3. **Оновлення CoreSettingsSection:**
```tsx
const { t } = useTranslations();

<h2>{t('settings.core.title')}</h2>
<p>{t('settings.core.subtitle')}</p>
<label>{t('settings.core.language')}</label>
```

## 🎯 Рекомендація

**Використати Варіант Б:**
- Мінімальні зміни
- Працює з поточною структурою
- Легко розширювати переклади
- Інтеграція з Settings API вже готова

**Не використовувати Варіант А поки що:**
- Занадто великий рефакторинг
- Ризик зламати робочий функціонал
- Можна зробити пізніше якщо потрібен SEO

## 📝 Готові JSON-переклади

Всі переклади вже готові в:
- `/locales/pl/common.json` - польська
- `/locales/en/common.json` - англійська
- `/locales/ru/common.json` - російська

Структура:
```json
{
  "settings": {
    "title": "...",
    "core": { "language": "..." },
    "ai": { "mentorStyle": "..." },
    "notifications": { "important": "..." }
  },
  "profile": { "settings": "..." },
  "common": { "save": "...", "cancel": "..." }
}
```

## ✅ Що працює зараз

1. **Settings API** - зберігає мову користувача
2. **LanguageContext** - синхронізується з Settings
3. **JSON-переклади** - готові для всіх мов
4. **Fallback** - browser language → pl

## 🚀 Швидкий старт

Створи простий хук і почни використовувати:
```bash
# 1. Створи lib/use-translations.ts (код вище)
# 2. Замени useLanguage на useTranslations в Settings
# 3. Протестуй зміну мови
# 4. Поступово мігруй інші компоненти
```
