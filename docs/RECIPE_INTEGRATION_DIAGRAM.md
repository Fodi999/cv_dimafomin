# 🎯 Recipe Integration Diagram

## 🔄 Полный поток: От рецепта к меню к готовке

```
┌────────────────────────────────────────────────────────────────┐
│                    📱 USER JOURNEY                             │
└────────────────────────────────────────────────────────────────┘

        🟡 ЭТАП 1: АССИСТЕНТ
        
┌─────────────────────────────────────┐
│ /assistant (AI Recommendations)     │
│                                     │
│  [🖼️ Recipe Card]                  │
│  ├─ Image                           │
│  ├─ Title: "Жареные яйца"          │
│  ├─ Cook time: 10 мин              │
│  ├─ Status: 🟢 Можно готовить      │
│  │                                 │
│  └─ [❤️] [👁️ Смотреть]            │
│                                     │
│  User clicks: ❤️                   │
└─────────────────────────────────────┘
                 │
                 ↓ (handleSaveRecipe)
                 │
        POST /api/user/recipes/save
        { recipeId: "uuid" }
                 │
                 ↓
┌─────────────────────────────────────┐
│ Backend: /api/user/recipes/save     │
│                                     │
│ CREATE MenuItem {                   │
│   id: "menu-item-uuid",             │
│   recipe_id: "recipe-uuid",         │
│   user_id: "user-id",               │
│   status: "menu" ← 🔥 KEY!          │
│   servings: 2,                      │
│   planned_for: TODAY,               │
│   created_at: NOW()                 │
│ }                                   │
│                                     │
│ Response: {                         │
│   success: true,                    │
│   menuItem: {...}                   │
│ }                                   │
└─────────────────────────────────────┘
                 │
                 ↓
        window.dispatchEvent(
          new Event('recipe-saved')
        )
                 │
                 ↓
         Toast: "✅ Рецепт"
         "добавлен в меню!"
                 │
                 ├─ event propagates to...
                 │
                 ↓
        
        🔵 ЭТАП 2: KITCHEN DASHBOARD
        
┌─────────────────────────────────────┐
│ /recipes (RecipesPage)              │
│                                     │
│ useEffect: addEventListener(        │
│   'recipe-saved',                   │
│   () => loadTodayMenu()             │
│ )                                   │
│                                     │
│ Receives event ← recipe-saved       │
└─────────────────────────────────────┘
                 │
                 ↓ loadTodayMenu()
                 │
        GET /api/menu/today
                 │
                 ↓
┌─────────────────────────────────────┐
│ Backend: /api/menu/today            │
│                                     │
│ SELECT * FROM menu_items            │
│ WHERE user_id = ?                   │
│   AND planned_for = TODAY           │
│                                     │
│ Response: MenuItem[] [              │
│   {                                 │
│     id: "menu-item-uuid",          │
│     status: "menu",                │
│     recipe: {                      │
│       title: "Жареные яйца",       │
│       image_url: "...",            │
│       cook_time: 10,               │
│       ...                          │
│     },                             │
│     servings: 2,                   │
│     created_at: ...                │
│   }                                │
│ ]                                  │
└─────────────────────────────────────┘
                 │
                 ↓
        setMenu(items)
        filtration:
        const menuItems =
          menu.filter(i.status === "menu")
                 │
                 ↓
        activeTab === 'menu' ?
        RENDER SECTION
                 │
                 ↓
┌─────────────────────────────────────┐
│ 📋 В МЕНЮ Tab                       │
│                                     │
│ ┌──────────────────────────────┐   │
│ │ 🍳 Жареные яйца              │   │
│ │ [🖼️ Image]                   │   │
│ │                              │   │
│ │ ⏱️ 10 мин                    │   │
│ │                              │   │
│ │ Порции: [2 ▾]                │   │
│ │                              │   │
│ │ [Удалить] [🍳 Готовить]     │   │
│ └──────────────────────────────┘   │
│                                     │
│ Stats: [2] В меню                   │
└─────────────────────────────────────┘
                 │
                 ↓ User clicks "🍳 Готовить"
                 │
        handleStartCooking(itemId)
                 │
                 ↓
        POST /api/menu/{id}/start
                 │
                 ↓
┌─────────────────────────────────────┐
│ Backend: /api/menu/{id}/start       │
│                                     │
│ UPDATE menu_items                   │
│ SET status = "cooking",             │
│     started_cooking_at = NOW()      │
│ WHERE id = ?                        │
│                                     │
│ Response: MenuItem {                │
│   status: "cooking" ← 🔥 CHANGED!   │
│ }                                   │
└─────────────────────────────────────┘
                 │
                 ↓ Toast: "Начали готовить!"
                 │
        loadTodayMenu()
                 │
                 ↓ GET /api/menu/today
                 │
                 ↓
┌─────────────────────────────────────┐
│ 🍳 ГОТОВИТСЯ Tab                    │
│                                     │
│ ┌──────────────────────────────┐   │
│ │ 🍳 Жареные яйца              │   │
│ │ [🖼️ Image with 💫 pulse]     │   │
│ │                              │   │
│ │ 🕐 Готовим с 15:45          │   │
│ │                              │   │
│ │ [✅ Готово!]                 │   │
│ └──────────────────────────────┘   │
│                                     │
│ Stats: [1] Готовится                │
└─────────────────────────────────────┘
                 │
                 ↓ User clicks "✅ Готово!"
                 │
        handleCompleteCooking(itemId)
                 │
                 ↓
        POST /api/menu/{id}/complete
                 │
                 ↓
┌─────────────────────────────────────┐
│ Backend: /api/menu/{id}/complete    │
│                                     │
│ UPDATE menu_items                   │
│ SET status = "history",             │
│     completed_at = NOW()            │
│ WHERE id = ?                        │
│                                     │
│ Response: MenuItem {                │
│   status: "history" ← 🔥 CHANGED!   │
│ }                                   │
└─────────────────────────────────────┘
                 │
                 ↓ Toast: "Блюдо готово! 🎉"
                 │
        loadTodayMenu()
                 │
                 ↓ GET /api/menu/today
                 │
                 ↓
┌─────────────────────────────────────┐
│ ✅ ИСТОРИЯ Tab                      │
│ (collapsed by default)              │
│                                     │
│ Click to expand...                  │
│ ┌──────────────────────────────┐   │
│ │ 🍳 Жареные яйца              │   │
│ │ [🖼️ Image with 💫 pulse]     │   │
│ │                              │   │
│ │ ✅ Завершено в 15:52         │   │
│ │ 2 порции                     │   │
│ │                              │   │
│ │ (read-only)                  │   │
│ └──────────────────────────────┘   │
│                                     │
│ Stats: [1] История                  │
└─────────────────────────────────────┘
```

---

## 🔀 State Transitions

```
┌─────────┐
│  menu   │ ← Рецепт добавлен в меню (status === "menu")
└────┬────┘   Карточка редактируемая, можно менять порции
     │        Кнопка: "🍳 Готовить"
     │
     ↓ User clicks "Готовить"
     │
     POST /api/menu/{id}/start
     │
┌────────────┐
│  cooking   │ ← Готовка началась (status === "cooking")
└────┬───────┘   Карточка показывает время начала
     │            Кнопка: "✅ Готово!"
     │
     ↓ User clicks "Готово!"
     │
     POST /api/menu/{id}/complete
     │
┌─────────┐
│ history │ ← Готовка завершена (status === "history")
└─────────┘   Карточка read-only, показывает время завершения
              В отдельном свёрнутом табе "✅ История"
```

---

## 📱 UI Structure

```
RecipesPage
├─ Header
│  ├─ "Kitchen Dashboard" title
│  └─ Tab Navigation
│     ├─ 📋 В меню [2]      ← Count of "menu" items
│     ├─ 🍳 Готовится [1]   ← Count of "cooking" items
│     └─ ✅ История [3]     ← Count of "history" items
│
└─ Content (based on activeTab)
   │
   ├─ Tab: 'menu'
   │  └─ 📋 Сегодняшнее меню
   │     └─ Grid of MenuRecipeCard (status="menu")
   │        ├─ Image
   │        ├─ Title
   │        ├─ Cook time
   │        ├─ Servings selector
   │        └─ [🍳 Готовить] button
   │
   ├─ Tab: 'cooking'
   │  └─ 🍳 Активная готовка
   │     └─ Grid of MenuRecipeCard (status="cooking")
   │        ├─ Image with pulsing border
   │        ├─ Title
   │        ├─ Start time
   │        └─ [✅ Готово!] button
   │
   └─ Tab: 'history'
      └─ ✅ Приготовлено сегодня
         └─ Grid of MenuRecipeCard (status="history")
            ├─ Image with pulsing border
            ├─ Title
            ├─ Completion time
            └─ (read-only, no buttons)
```

---

## 🔗 API Contract

### POST /api/user/recipes/save
**Добавить рецепт в меню**

```http
POST /api/user/recipes/save
Authorization: Bearer {token}
Content-Type: application/json

{
  "recipeId": "uuid-of-recipe"
}
```

**Response (200 OK):**
```json
{
  "success": true,
  "menuItem": {
    "id": "menu-item-uuid",
    "recipe_id": "recipe-uuid",
    "status": "menu",
    "servings": 2,
    "planned_for": "2026-01-22",
    "created_at": "2026-01-22T15:30:00Z",
    "recipe": {
      "id": "recipe-uuid",
      "title": "Жареные яйца",
      "image_url": "https://...",
      "cook_time": 10,
      "servings": 2
    }
  }
}
```

---

### GET /api/menu/today
**Получить меню на сегодня**

```http
GET /api/menu/today
Authorization: Bearer {token}
```

**Response (200 OK):**
```json
[
  {
    "id": "menu-item-uuid-1",
    "recipe_id": "recipe-uuid-1",
    "status": "menu",
    "servings": 2,
    "planned_for": "2026-01-22",
    "created_at": "2026-01-22T15:30:00Z",
    "recipe": {
      "id": "recipe-uuid-1",
      "title": "Жареные яйца",
      "image_url": "https://...",
      "cook_time": 10,
      "servings": 2
    }
  },
  {
    "id": "menu-item-uuid-2",
    "recipe_id": "recipe-uuid-2",
    "status": "cooking",
    "servings": 3,
    "started_cooking_at": "2026-01-22T15:45:00Z",
    "recipe": {
      "id": "recipe-uuid-2",
      "title": "Борщ",
      "image_url": "https://...",
      "cook_time": 45,
      "servings": 4
    }
  },
  {
    "id": "menu-item-uuid-3",
    "recipe_id": "recipe-uuid-3",
    "status": "history",
    "servings": 2,
    "completed_at": "2026-01-22T16:15:00Z",
    "recipe": {
      "id": "recipe-uuid-3",
      "title": "Салат",
      "image_url": "https://...",
      "cook_time": 15,
      "servings": 2
    }
  }
]
```

---

### POST /api/menu/{id}/start
**Начать готовку**

```http
POST /api/menu/{menu-item-id}/start
Authorization: Bearer {token}
```

**Response (200 OK):**
```json
{
  "id": "menu-item-uuid",
  "status": "cooking",
  "started_cooking_at": "2026-01-22T15:45:00Z"
}
```

---

### POST /api/menu/{id}/complete
**Завершить готовку**

```http
POST /api/menu/{menu-item-id}/complete
Authorization: Bearer {token}
```

**Response (200 OK):**
```json
{
  "id": "menu-item-uuid",
  "status": "history",
  "completed_at": "2026-01-22T16:15:00Z"
}
```

---

### PATCH /api/menu/{id}
**Обновить порции**

```http
PATCH /api/menu/{menu-item-id}
Authorization: Bearer {token}
Content-Type: application/json

{
  "servings": 3
}
```

**Response (200 OK):**
```json
{
  "id": "menu-item-uuid",
  "servings": 3
}
```

---

## 🎯 Key Points

✅ **Статус "menu"** означает: рецепт добавлен в меню, но готовка ещё не началась  
✅ **Event 'recipe-saved'** автоматически триггерит перезагрузку меню на /recipes  
✅ **Фильтрация** происходит на фронте по status  
✅ **Backend — источник истины** (SSOT), фронт только отображает  
✅ **3 состояния** = 3 таба на UI  

❌ **НЕ** надо: манипулировать статусом на фронте  
❌ **НЕ** надо: использовать localStorage для статуса  
❌ **НЕ** надо: дублировать логику фильтрации  

