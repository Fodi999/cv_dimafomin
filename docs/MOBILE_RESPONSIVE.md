# Mobile Responsive: Journey Pages & Admin Panel

## 🎯 Цель

Адаптировать все 6 страниц journey и админ-панель для комфортного использования на мобильных устройствах.

---

## ✅ Что адаптировано

### 1️⃣ **Journey Pages (Главная страница)**

**Файл:** `/app/page.tsx`

#### Desktop vs Mobile
| Элемент | Desktop | Mobile |
|---------|---------|--------|
| **Stats Counter** | `top-20` | `top-16` |
| **Content Padding** | `pt-16 pb-20` | `pt-20 pb-24 px-4` |
| **Marquee Bar** | Видна | Скрыта (`hidden sm:block`) |
| **Navigation Buttons** | Полный текст | Иконки/короткий текст |
| **Progress Dots** | `w-2 h-2 gap-2` | `w-1.5 h-1.5 gap-1.5` |
| **Footer Padding** | `p-4` | `p-3` |

#### Мобильные оптимизации

**Бегущая строка (Marquee):**
```tsx
<div className="hidden sm:block ...">
  {/* Скрыта на мобильных для экономии места */}
</div>
```

**Кнопка "Назад":**
```tsx
<span className="hidden sm:inline">На старт</span>
<span className="sm:hidden">🏠</span>
```

**Кнопка "Предыдущая":**
```tsx
<span className="hidden sm:inline">{t.journey.navigation.back}</span>
<span className="sm:hidden">←</span>
```

**Кнопка "Далее":**
```tsx
<span className="hidden sm:inline">{t.journeyNextButtonTexts.screen1}</span>
<span className="sm:hidden">Далее</span>
```

**Кнопка "Завершить":**
```tsx
<span className="hidden sm:inline">{t.journey.navigation.finish}</span>
<span className="sm:hidden">✓</span>
```

---

### 2️⃣ **Admin: Add Ingredient Dialog**

**Файл:** `/components/admin/catalog/ingredients/AddIngredientDialog.tsx`

#### Адаптивные изменения

**Кнопка триггера:**
```tsx
<Button size="sm" className="w-full sm:w-auto">
  <Plus className="mr-2 h-4 w-4" />
  <span className="hidden sm:inline">{t.admin.catalog.products.addProduct}</span>
  <span className="sm:hidden">Добавить</span>
</Button>
```

**Модальное окно:**
```tsx
<DialogContent className="sm:max-w-[500px] max-w-[95vw] max-h-[90vh] overflow-y-auto">
```
- Desktop: 500px ширина
- Mobile: 95% viewport width, 90% высоты, скролл

**Заголовок:**
```tsx
<DialogTitle className="text-lg sm:text-xl">
```

**Описание:**
```tsx
<DialogDescription className="text-xs sm:text-sm">
```

**Placeholder:**
```tsx
placeholder="Например: Avocado, Огурец"  // Короче на мобильных
```

**Предупреждение о дубликате:**
```tsx
<div className="flex flex-col sm:flex-row ...">
  {/* Вертикальная раскладка на мобильных */}
  <div className="flex flex-col sm:flex-row sm:items-center gap-1.5 sm:gap-2 ...">
```

**Кнопка "Использовать":**
```tsx
<Button className="h-8 sm:h-7 text-xs w-full sm:w-auto">
  Использовать
</Button>
```
- Mobile: `w-full` (на всю ширину)
- Desktop: `w-auto`

**Footer кнопки:**
```tsx
<DialogFooter className="flex-col sm:flex-row gap-2">
  <Button className="w-full sm:w-auto order-2 sm:order-1">
    Отмена
  </Button>
  <Button className="w-full sm:w-auto order-1 sm:order-2">
    Создать
  </Button>
</DialogFooter>
```
- Mobile: Вертикальная раскладка, основная кнопка сверху
- Desktop: Горизонтальная раскладка

---

### 3️⃣ **Admin: Ingredients Table**

**Файл:** `/components/admin/catalog/ingredients/IngredientsTable.tsx`

#### Desktop: Таблица
```tsx
<div className="hidden md:block overflow-x-auto">
  <table>...</table>
</div>
```

#### Mobile: Карточки
```tsx
<div className="md:hidden divide-y ...">
  {ingredients.map(ingredient => (
    <div className="p-4">
      <h3>{ingredient.name}</h3>
      <div className="flex gap-1.5">
        <Badge>{category}</Badge>
        <Badge>{unit}</Badge>
        <Badge>{age}</Badge>
      </div>
      <div className="flex items-center gap-3 text-xs">
        <span>{usageCount} recipes</span>
        <span>{createdAt}</span>
      </div>
      <div className="flex gap-1">
        <Button size="sm" />
        <Button size="sm" />
      </div>
    </div>
  ))}
</div>
```

**Преимущества карточек:**
- ✅ Вертикальная раскладка
- ✅ Вся информация видна
- ✅ Крупные кнопки (легко нажать)
- ✅ Цветовая подсветка (today/new) работает
- ✅ Компактно, но читаемо

---

## 📱 Брейкпоинты

| Breakpoint | Width | Применение |
|------------|-------|------------|
| `sm:` | ≥640px | Малые планшеты и больше |
| `md:` | ≥768px | Планшеты в портретной ориентации |
| `lg:` | ≥1024px | Ноутбуки |
| `xl:` | ≥1280px | Десктопы |

**Основная логика:**
- `hidden sm:block` — скрыть на мобильных
- `sm:hidden` — показать только на мобильных
- `w-full sm:w-auto` — полная ширина на мобильных

---

## 🎨 Mobile-First Стратегия

### Принципы

1. **Touch-Friendly**
   - Минимальная высота кнопок: `h-8` (32px)
   - Gaps между элементами: `gap-2` (8px)
   - Увеличенные иконки: `w-3 h-3` → `w-4 h-4`

2. **Читаемость**
   - Минимальный размер текста: `text-xs` (12px)
   - Контрастные цвета
   - Увеличенные отступы: `p-3 sm:p-4`

3. **Навигация**
   - Фиксированный футер
   - Крупные кнопки навигации
   - Прогресс всегда виден

4. **Контент**
   - Padding слева/справа: `px-4`
   - Скролл для длинного контента
   - Модальные окна: `max-h-[90vh]`

---

## 🧪 Тестирование

### Desktop (≥640px)
- ✅ Бегущая строка видна
- ✅ Полный текст кнопок
- ✅ Таблица ingredients
- ✅ Горизонтальная раскладка модалок

### Mobile (<640px)
- ✅ Бегущая строка скрыта
- ✅ Иконки/короткий текст кнопок
- ✅ Карточки ingredients
- ✅ Вертикальная раскладка модалок
- ✅ Кнопки на всю ширину

### Touch Testing
- ✅ Все кнопки ≥32px высотой
- ✅ Между кнопками ≥8px gap
- ✅ Иконки ≥16px (легко различить)

---

## 📐 Размеры элементов

### Кнопки
| Тип | Mobile | Desktop |
|-----|--------|---------|
| Primary | `h-8 px-3` | `h-7 px-6` |
| Icon | `h-8 w-8` | `h-7 w-7` |
| Text | `text-xs` | `text-sm` |

### Spacing
| Элемент | Mobile | Desktop |
|---------|--------|---------|
| Padding | `p-3` | `p-4` |
| Gap | `gap-1.5` | `gap-2` |
| Margin | `mt-2` | `mt-3` |

### Typography
| Элемент | Mobile | Desktop |
|---------|--------|---------|
| Title | `text-lg` | `text-xl` |
| Body | `text-xs` | `text-sm` |
| Badge | `text-xs` | `text-xs` |

---

## 🔮 Будущие улучшения

### Phase 2: Touch Gestures
```tsx
// Swipe для навигации между экранами
<Swipeable
  onSwipedLeft={() => nextScreen()}
  onSwipedRight={() => prevScreen()}
>
```

### Phase 3: Вибрация
```tsx
// Тактильный фидбек на кнопках
onClick={() => {
  navigator.vibrate?.(10);
  handleClick();
}}
```

### Phase 4: PWA Install Prompt
```tsx
// Предложить установить как приложение
{isPWA && <InstallPrompt />}
```

---

## ✅ Checklist

- ✅ Journey pages адаптированы
- ✅ Бегущая строка скрыта на мобильных
- ✅ Кнопки навигации компактные
- ✅ Прогресс индикатор уменьшен
- ✅ Admin dialog адаптирован
- ✅ Ingredients table → карточки
- ✅ Touch-friendly размеры
- ✅ Вертикальная раскладка модалок
- ✅ Кнопки на всю ширину
- ✅ Скролл для длинного контента

---

## 🎯 Итог

**До:**
- ❌ Таблица не влезает на мобильных
- ❌ Мелкий текст
- ❌ Сложно нажимать кнопки
- ❌ Модалки обрезаны

**После:**
- ✅ Карточки вместо таблицы
- ✅ Читаемый размер текста
- ✅ Крупные touch-friendly кнопки
- ✅ Модалки адаптированы

🚀 **Готово к использованию на всех устройствах!**
