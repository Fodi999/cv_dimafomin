# RecipeCard Components Analysis

**Дата анализа**: 28 декабря 2025  
**Статус**: 📊 Зафиксировано

---

## 📋 Обзор

В проекте существует **3 различных RecipeCard компонента** с разным назначением:

| Компонент | Размер | Назначение | Статус |
|-----------|--------|------------|--------|
| `assistant/RecipeCard.tsx` | 292 строки | AI-рекомендации из холодильника | ✅ Активен |
| `market/RecipeCard.tsx` | 128 строк | Маркетплейс рецептов | ✅ Активен |
| `recipes/RecipeCard.tsx` | 164 строки | Общий список рецептов | ✅ Активен |

---

## 🔍 Детальный анализ

### 1. `components/assistant/RecipeCard.tsx` (292 строки)

**Назначение**: AI Assistant - рекомендации рецептов на основе продуктов из холодильника

**Props**:
```tsx
interface RecipeCardProps {
  recipe: Recipe;                    // Полный объект рецепта
  onAddToPlan?: (recipe: Recipe) => void;  // Добавить в план
  onMarkDone?: (recipe: Recipe) => void;   // Отметить как выполненное
  loading?: boolean;                 // Индикатор загрузки
}
```

**Уникальные особенности**:
- ✅ Показывает **экономические данные** (`recipe.economy`)
- ✅ Badge для **приоритета истечения срока** продуктов (`expiryPriority`)
- ✅ Отображает **процент совпадения** с холодильником
- ✅ Кнопки действий: "Add to Plan", "Mark Done"
- ✅ Расширенная информация о калориях, времени готовки

**Контекст использования**: 
- `/assistant` - страница AI-помощника
- Компонент интегрирован с холодильником и анализом AI

**Зависимости**:
- `lib/types.ts` (Recipe type)
- Иконки: `Clock`, `Flame`, `ChefHat`, `TrendingUp`, `CheckCircle2`

---

### 2. `components/market/RecipeCard.tsx` (128 строк)

**Назначение**: Маркетплейс - карточка рецепта для покупки

**Props**:
```tsx
interface RecipeCardProps {
  id: string;
  title: string;
  description: string;
  price: number;                     // 💰 ЦЕНА (уникально!)
  rating: number;                    // ⭐ Рейтинг
  studentsCount: number;             // Количество студентов
  image: string;
  author: string;
  difficulty: "beginner" | "intermediate" | "advanced";
}
```

**Уникальные особенности**:
- ✅ Отображает **цену в ChefTokens** 💰
- ✅ Показывает **рейтинг** и количество студентов
- ✅ Кнопка **"Buy Recipe"** → покупка
- ✅ Компактный дизайн для листинга
- ✅ Навигация: `router.push(/market/${id})`

**Контекст использования**:
- `/market` - страница маркетплейса
- Компонент ориентирован на монетизацию

**Зависимости**:
- `next/image`, `next/navigation`
- Иконки: `Star`, `Users`, `Clock`
- ChefToken система

---

### 3. `components/recipes/RecipeCard.tsx` (164 строки)

**Назначение**: Общий список рецептов (feed, профиль)

**Props**:
```tsx
export interface RecipeCardProps {
  id: string;
  title: string;
  imageUrl: string;
  author: {
    name: string;
    avatar?: string;              // 👤 Аватар автора (уникально!)
  };
  difficulty?: "beginner" | "intermediate" | "advanced";
  likes?: number;                 // ❤️ Лайки (уникально!)
  comments?: number;              // 💬 Комментарии (уникально!)
  cookingTime?: number;
  category?: string;
}
```

**Уникальные особенности**:
- ✅ Информация об **авторе** с аватаром
- ✅ Социальные метрики: **лайки**, **комментарии**
- ✅ Категория рецепта
- ✅ Hover эффекты для интерактивности
- ✅ Навигация: `router.push(/recipes/${id})`

**Контекст использования**:
- `/recipes` - список всех рецептов
- Профили пользователей
- Feed лента

**Зависимости**:
- `next/image`, `next/navigation`
- Иконки: `Clock`, `Heart`, `MessageCircle`, `ChefHat`
- UI компоненты: `Card`, `Badge`, `Avatar`

---

## 🎯 Сравнительная таблица

| Функция | Assistant | Market | Recipes |
|---------|-----------|--------|---------|
| Цена в токенах | ❌ | ✅ | ❌ |
| Рейтинг | ❌ | ✅ | ❌ |
| Лайки/Комментарии | ❌ | ❌ | ✅ |
| Автор с аватаром | ❌ | Имя | ✅ Имя + аватар |
| Экономика рецепта | ✅ | ❌ | ❌ |
| Expiry Priority | ✅ | ❌ | ❌ |
| Процент совпадения | ✅ | ❌ | ❌ |
| Action buttons | Add/Done | Buy | View |
| Сложность | ✅ | ✅ | ✅ |
| Время готовки | ✅ | ✅ | ✅ |
| Калории | ✅ | ❌ | ❌ |

---

## 💡 Выводы

### ✅ Различия обоснованы

Все три компонента имеют **разное назначение** и **разный контекст**:

1. **Assistant RecipeCard** - сфокусирован на AI-анализе и холодильнике
2. **Market RecipeCard** - сфокусирован на монетизации
3. **Recipes RecipeCard** - сфокусирован на социальных функциях

### 🎨 Общие элементы

Можно выделить в shared компоненты:

```tsx
// components/recipes/shared/
├── RecipeDifficultyBadge.tsx     // Все 3 используют
├── RecipeCookingTime.tsx         // Все 3 используют
└── RecipeImage.tsx               // Можно унифицировать
```

### 🔄 Рекомендации

#### Вариант 1: Оставить как есть (рекомендуется)
- ✅ Каждый компонент четко специализирован
- ✅ Легко поддерживать отдельно
- ✅ Нет излишней сложности

**Действие**: Добавить комментарии в код

#### Вариант 2: Выделить общие части (опционально)
```tsx
// components/recipes/base/
├── BaseRecipeCard.tsx            // Общая структура
├── RecipeCardHeader.tsx          // Title + difficulty
├── RecipeCardMeta.tsx            // Time, author, stats
└── RecipeCardActions.tsx         // Buttons (разные для каждого)
```

**Плюсы**: Меньше дублирования  
**Минусы**: Больше абстракций, сложнее читать

---

## 📝 Решение

### ✅ Фиксируем текущую структуру

**Статус**: Различия обоснованы и будут сохранены

**Действия**:
1. ✅ Документировать различия (этот файл)
2. ⏳ Добавить JSDoc комментарии в каждый файл
3. ⏳ Извлечь общие UI элементы (badges, time)

---

## 🛠️ Добавление JSDoc комментариев

### assistant/RecipeCard.tsx
```tsx
/**
 * RecipeCard для AI Assistant
 * 
 * Показывает AI-рекомендации рецептов на основе анализа холодильника.
 * Включает экономические данные, приоритет истечения срока продуктов,
 * процент совпадения с имеющимися ингредиентами.
 * 
 * @usage Используется на странице /assistant
 * @features
 * - Economy data (cost, savings)
 * - Expiry priority badge
 * - Match percentage
 * - Actions: Add to Plan, Mark Done
 */
```

### market/RecipeCard.tsx
```tsx
/**
 * RecipeCard для Marketplace
 * 
 * Карточка рецепта в маркетплейсе для покупки.
 * Показывает цену в ChefTokens, рейтинг, количество студентов.
 * 
 * @usage Используется на странице /market
 * @features
 * - Price in ChefTokens
 * - Rating & students count
 * - Buy button
 * - Compact design for listing
 */
```

### recipes/RecipeCard.tsx
```tsx
/**
 * RecipeCard для общего списка рецептов
 * 
 * Универсальная карточка для отображения рецептов в feed,
 * профилях пользователей, списках. Включает социальные метрики.
 * 
 * @usage Используется на /recipes, в профилях, feed
 * @features
 * - Author with avatar
 * - Social metrics (likes, comments)
 * - Category
 * - Hover effects
 */
```

---

## 🎯 Action Items

- [x] Провести анализ трёх RecipeCard
- [x] Зафиксировать различия в документации
- [ ] Добавить JSDoc комментарии в каждый компонент
- [ ] Извлечь общие UI элементы (RecipeDifficultyBadge, RecipeCookingTime)
- [ ] Рассмотреть использование design tokens для консистентности

---

## 📊 Метрики

- **Всего RecipeCard компонентов**: 3
- **Общие строки кода**: 584
- **Уникальный функционал**: ~70%
- **Общий функционал**: ~30% (difficulty, time, image)

**Вывод**: Дублирование минимальное, специализация оправдана ✅

---

*Последнее обновление: 28 декабря 2025*  
*Статус: ✅ Проанализировано и зафиксировано*
