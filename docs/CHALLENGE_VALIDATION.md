# Challenge Question Validation

## 🔹 Проблема (была)

Раньше можно было сохранить:
- ❌ Пустой вопрос
- ❌ Пустой ответ  
- ❌ Токены = 0
- ❌ Некорректная сложность

**Результат**: мусор в базе данных, плохой UX для пользователей.

---

## ✅ Решение (реализовано)

### Валидация перед сохранением

```typescript
// components/admin/challenges/ChallengeFormSteps.tsx

const canSaveQuestion =
  newQuestion.text.trim().length > 10 &&       // Минимум 10 символов
  newQuestion.correctAnswer.trim().length > 3 && // Минимум 3 символа
  newQuestion.level >= 1 &&                     // Уровень от 1
  newQuestion.level <= 5;                       // до 5
```

### Визуальные подсказки

1. **Счетчик символов** под каждым полем:
   - 🟢 Зеленый: требования выполнены
   - 🔴 Красный: недостаточно символов
   - ⚪ Серый: поле пустое

2. **Блокировка кнопок**:
   ```tsx
   <Button 
     disabled={!canSaveQuestion}
     title="Заповніть всі обовʼязкові поля правильно"
   >
     Зберегти
   </Button>
   ```

3. **Красная обводка** у невалидных полей

---

## 📋 Правила валидации

| Поле | Требование | Причина |
|------|------------|---------|
| **Текст вопроса** | ≥ 10 символов | Слишком короткие вопросы бессмысленны |
| **Правильный ответ** | ≥ 3 символа | Минимальная осмысленная длина |
| **Уровень сложности** | 1–5 | Определено в системе |
| **Тип ответа** | Enum | `"text" \| "short" \| "number"` |

---

## 🎯 Улучшения для будущего

### TODO: Заменить alert → shadcn Toast

```typescript
// Сейчас (MVP):
alert("Перевірте всі поля...");

// Планируется:
import { toast } from "@/components/ui/use-toast";

toast({
  title: "Помилка валідації",
  description: "Питання має бути мінімум 10 символів",
  variant: "destructive",
});
```

### TODO: Дополнительная валидация

- [ ] Проверка на дубликаты вопросов
- [ ] Валидация формата чисел (для `answerType: "number"`)
- [ ] Проверка длины пояснения (если заполнено)
- [ ] Асинхронная проверка уникальности через API

---

## 🔍 Как тестировать

1. Открыть `/admin/challenges/create`
2. Перейти на шаг "Питання"
3. Нажать "Додати питання"
4. Попытаться сохранить:
   - ✅ Пустую форму → кнопки заблокированы
   - ✅ Короткий вопрос (5 символов) → красная обводка + счетчик
   - ✅ Короткий ответ (1 символ) → красная обводка + счетчик
   - ✅ Заполненную форму → кнопки активны, сохранение работает

---

## 📝 Связанные файлы

- `components/admin/challenges/ChallengeFormSteps.tsx` - компонент формы
- `lib/challenges/types.ts` - определения типов
- `app/admin/challenges/page.tsx` - список челенджів (улучшен loading)

---

**Статус**: ✅ Реализовано (MVP)  
**Дата**: 13 января 2026  
**Следующий шаг**: Заменить `alert()` на `shadcn Toast` для лучшего UX
