# 🔍 Система фильтрации рецептов

## 📋 Обзор

Реализована полнофункциональная система фильтрации и сортировки рецептов в админ-панели каталога.

## 🎯 Функциональность

### 1️⃣ Поисковый фильтр
- **Поле**: Текстовый поиск по названию
- **Placeholder**: "Пошук за назвою..."
- **Иконка**: 🔍 Search
- **Поведение**: Поиск в реальном времени (с debounce на бэкенде)

### 2️⃣ Фильтр по кухне/категории
**Доступные опции**:
- 🌍 Всі категорії (по умолчанию)
- 🇮🇹 Італійська
- 🇯🇵 Японська
- 🇺🇦 Українська
- 🇨🇳 Китайська
- 🇫🇷 Французька
- 🇺🇸 Американська
- 🇲🇽 Мексиканська
- 🇮🇳 Індійська
- 🇹🇭 Тайська
- 🌊 Середземноморська
- 🥢 Азійська
- 🌍 Європейська

### 3️⃣ Фильтр по сложности
**Опции**:
- ⭐ Всі складності
- 🟢 Легкий (`easy`)
- 🟡 Середній (`medium`)
- 🔴 Складний (`hard`)

### 4️⃣ Фильтр по статусу
**Опции**:
- 📊 Всі статуси
- 📝 Чернетка (`draft`)
- ✅ Опубліковано (`published`)
- 📦 Архів (`archived`)

### 5️⃣ Сортировка
**По полю** (sortBy):
- 📅 За датою створення (`created_at`) - по умолчанию
- 🔤 За назвою (`title`)
- ⏱️ За часом приготування (`cooking_time`)
- 👁️ За переглядами (`views`)

**Направление** (sortOrder):
- ⬇️ За спаданням (`desc`) - по умолчанию
- ⬆️ За зростанням (`asc`)

## 🎨 UI/UX фичи

### Счётчик активных фильтров
```tsx
{activeFiltersCount > 0 && (
  <Badge variant="secondary">
    {activeFiltersCount}
  </Badge>
)}
```

### Кнопка сброса
Появляется только когда есть активные фильтры:
```tsx
{activeFiltersCount > 0 && (
  <Button onClick={handleResetFilters}>
    <X className="h-3 w-3" />
    Скинути все
  </Button>
)}
```

### Теги активных фильтров
Показываются под фильтрами, каждый с кнопкой удаления:
```tsx
<Badge variant="secondary">
  Пошук: "{filters.search}"
  <X onClick={() => removeFilter('search')} />
</Badge>
```

## 🔧 TypeScript интерфейсы

```typescript
export interface RecipesFilters {
  search: string;
  cuisine: string;         // 'all' | 'italian' | 'japanese' | ...
  difficulty: string;      // 'all' | 'easy' | 'medium' | 'hard'
  status: string;          // 'all' | 'draft' | 'published' | 'archived'
  sortBy?: string;         // 'created_at' | 'title' | 'cooking_time' | 'views'
  sortOrder?: 'asc' | 'desc';
  page: number;
  limit: number;
}
```

## 📡 API запросы

### Пример URL с фильтрами
```
GET /api/admin/recipes?search=лосось&cuisine=japanese&difficulty=easy&status=published&sortBy=created_at&sortOrder=desc&page=1&limit=50
```

### Query параметры
| Параметр | Тип | Пример | Обязательный |
|----------|-----|--------|--------------|
| `search` | string | `лосось` | ❌ |
| `cuisine` | string | `japanese` | ❌ |
| `difficulty` | string | `easy` | ❌ |
| `status` | string | `published` | ❌ |
| `sortBy` | string | `created_at` | ❌ |
| `sortOrder` | string | `desc` | ❌ |
| `page` | number | `1` | ✅ |
| `limit` | number | `50` | ✅ |

## 🚀 Использование

### В компоненте
```tsx
import { useAdminRecipes } from '@/hooks/useAdminRecipes';

function RecipesTab() {
  const { 
    recipes, 
    filters, 
    updateFilters,
    isLoading 
  } = useAdminRecipes();

  // Обновить фильтр
  updateFilters({ cuisine: 'italian', page: 1 });

  // Сбросить все фильтры
  updateFilters({
    search: '',
    cuisine: 'all',
    difficulty: 'all',
    status: 'all',
    sortBy: 'created_at',
    sortOrder: 'desc',
    page: 1
  });
}
```

### Подсчёт активных фильтров
```typescript
const activeFiltersCount = [
  filters.search,
  filters.cuisine && filters.cuisine !== 'all',
  filters.difficulty && filters.difficulty !== 'all',
  filters.status && filters.status !== 'all'
].filter(Boolean).length;
```

## 📊 Результаты фильтрации

### Мета-информация
```typescript
interface RecipesMeta {
  total: number;      // Всего рецептов
  page: number;       // Текущая страница
  limit: number;      // Рецептов на странице
  totalPages: number; // Всего страниц
}
```

### Отображение счётчика
```tsx
<div className="text-sm text-muted-foreground text-center">
  Показано: {recipes.length} з {meta.total}
</div>
```

## 🎨 Стилизация

### Иконки для категорий
Каждая категория имеет эмодзи-иконку:
- 🇮🇹 Італійська
- 🇯🇵 Японська
- 🇺🇦 Українська

### Цветовая индикация сложности
- 🟢 Легкий - зелёный
- 🟡 Середній - жёлтый
- 🔴 Складний - красный

### Бейджи статусов
- 📝 Чернетка - серый
- ✅ Опубліковано - синий
- 📦 Архів - оранжевый

## 🧪 Тестирование

```typescript
// Тест фильтрации
const filters = {
  search: 'лосось',
  cuisine: 'japanese',
  difficulty: 'easy',
  status: 'published',
  sortBy: 'created_at',
  sortOrder: 'desc' as const,
  page: 1,
  limit: 50
};

// Проверка активных фильтров
const activeCount = countActiveFilters(filters);
console.assert(activeCount === 4); // search, cuisine, difficulty, status
```

## 🔄 Автоматическое поведение

1. **При изменении фильтра** → `page` сбрасывается на 1
2. **При сбросе всех фильтров** → возврат к дефолтным значениям
3. **При изменении сортировки** → `page` сбрасывается на 1
4. **Debounce на поиске** → запрос отправляется с задержкой 300ms

## 🎯 Примеры сценариев

### Сценарий 1: Поиск японских рецептов
```typescript
updateFilters({
  search: '',
  cuisine: 'japanese',
  difficulty: 'all',
  status: 'published',
  page: 1
});
```

### Сценарий 2: Новые рецепты сначала
```typescript
updateFilters({
  sortBy: 'created_at',
  sortOrder: 'desc',
  page: 1
});
```

### Сценарий 3: Легкие рецепты по алфавиту
```typescript
updateFilters({
  difficulty: 'easy',
  sortBy: 'title',
  sortOrder: 'asc',
  page: 1
});
```

---

**✨ Готово!** Фильтры работают с мгновенным обновлением, визуальными индикаторами и удобным UX.
