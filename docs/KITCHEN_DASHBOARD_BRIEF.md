# Kitchen Dashboard — Архитектурный обзор

## 🎯 Главная идея

Страница `/recipes` — это **Dashboard приготовления на день**, как в реальном ресторане:

```
УТРО         ДЕНЬ          ВЕЧЕР
 ↓            ↓              ↓
[Planned] → [Cooking] → [Completed]
```

## 📊 Три состояния рецепта

| Статус | Иконка | Название | Действие | Кнопка |
|--------|--------|----------|----------|--------|
| **planned** | 🟡 | Сегодня готовим | Просмотр + изменение порций | "Начать готовить" |
| **cooking** | 🔵 | В процессе | Просмотр деталей | "Готово!" |
| **completed** | ✅ | Приготовлено сегодня | Просмотр истории | — (readonly) |

## 🔗 Поток данных

```
Backend /api/menu/today
         ↓
    MenuItem[]
    [
      { id: "1", status: "planned", recipe: {...} },
      { id: "2", status: "cooking", recipe: {...} },
      { id: "3", status: "completed", recipe: {...} }
    ]
         ↓
Frontend Group by Status
         ↓
    planned = [MenuItem, ...]
    cooking = [MenuItem, ...]
    completed = [MenuItem, ...]
         ↓
    Render 3 Sections
```

## 🎬 Сценарий использования

### Пользователь добавляет рецепт

```
1. /assistant
2. Нажимает ❤️
3. POST /api/user/recipes/save
4. Рецепт добавляется → status: "planned"
```

### Пользователь готовит

```
1. /recipes
2. Видит карточку в "Сегодня готовим"
3. Нажимает "Начать готовить"
4. POST /api/menu/:id/start
5. status: "planned" → "cooking"
6. Карточка перемещается в "В процессе"
```

### Рецепт готов

```
1. Пользователь готовит...
2. Нажимает "Готово!"
3. POST /api/menu/:id/complete
4. status: "cooking" → "completed"
5. Карточка перемещается в "Приготовлено сегодня"
```

## 🏗️ Компоненты

```
/app/(user)/recipes/page.tsx
├─ loadTodayMenu()           ← Загружает данные
├─ handleStartCooking()      ← Изменяет статус
├─ handleCompleteCooking()   ← Завершает
├─ handleUpdateServings()    ← Обновляет порции
└─ Render 3 Sections
   ├─ Planned Section (всегда видна)
   ├─ Cooking Section (свёртываемо)
   └─ Completed Section (свёртываемо)

/components/recipes/MenuRecipeCard.tsx
└─ Отображает рецепт с зависимостью от статуса

/lib/api/menu.ts
├─ menuApi.getToday()           ← GET /api/menu/today
├─ menuApi.startCooking()       ← POST /api/menu/:id/start
├─ menuApi.completeCooking()    ← POST /api/menu/:id/complete
└─ menuApi.updateServings()     ← PATCH /api/menu/:id

/app/api/menu/
├─ today/route.ts              ← Route Handler
├─ [id]/start/route.ts         ← Route Handler
├─ [id]/complete/route.ts      ← Route Handler
└─ [id]/route.ts               ← Route Handler
```

## ✅ Правильная архитектура

**Source of Truth:** Backend `/api/menu/today`

```
✅ НЕ используем RecipeContext для этого
✅ НЕ сохраняем в localStorage
✅ Backend = единственный источник истины
✅ Frontend только отображает и фильтрует
```

## 🚀 Готовность

```
Backend:  ✅ Реализовано
Frontend: ✅ Реализовано
Route Handlers: ✅ Реализовано
UI Components: ✅ Реализовано
Логирование: ✅ Реализовано
Тестирование: 👉 Начинай отсюда!
```

## 📋 Чек-лист тестирования

- [ ] Добавить рецепт в меню (/assistant → ❤️)
- [ ] Открыть /recipes
- [ ] Карточка в "Сегодня готовим"? ✅
- [ ] Нажать "Начать готовить"
- [ ] Карточка в "В процессе"? ✅
- [ ] Нажать "Готово!"
- [ ] Карточка в "Приготовлено сегодня"? ✅
- [ ] Консоль без ошибок? ✅

**Если всё ✅ — ты готов к продакшену!** 🚀
