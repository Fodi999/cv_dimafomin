# ✅ ЯЗЫК: BACKEND КАК ЕДИНСТВЕННЫЙ ИСТОЧНИК ИСТИНЫ

**Статус:** ✅ Исправлено  
**Дата:** 16 января 2026

---

## 🎯 Что Исправлено

### Проблема:
```
❌ Cookie хранит язык независимо от backend
❌ UI может показывать один язык, AI использовать другой
❌ При смене языка backend не обновляется
```

### Решение:
```
✅ Backend = ЕДИНСТВЕННЫЙ источник истины
✅ Cookie = только кеш для SSR
✅ При загрузке: backend.language → cookie (синхронизация)
✅ При смене: backend first → затем cookie → затем UI
```

---

## 🔄 Как Работает Теперь

### 1️⃣ Первая загрузка (авторизованный пользователь)

```typescript
1. SettingsContext загружает настройки из backend
   GET /api/settings → { language: "ru", ... }

2. Сравнивает backend.language с cookie:
   - Если совпадают → ничего не делать
   - Если НЕ совпадают → обновить cookie + reload

3. ✅ Результат: backend и cookie синхронизированы
```

**Консоль:**
```
⚙️ Loading settings from backend...
✅ Settings loaded: { language: "ru", ... }
🔄 Language mismatch: cookie="pl", backend="ru"
🔄 Updating cookie to match backend: ru
🔄 Reloading page with correct language...
```

---

### 2️⃣ Смена языка пользователем

```typescript
1. User clicks "RU" в LanguageSwitcher

2. updateSettings({ language: "ru" })
   PATCH /api/settings → сохранить в БД

3. loadSettings()
   GET /api/settings → проверить синхронизацию с cookie

4. setLanguage("ru")
   → window.location.reload()

5. ✅ Вся страница перезагружается с новым языком
```

**Консоль:**
```
🌍 [1/3] Saving language to backend: ru
✅ [2/3] Language saved to DB: ru
🔄 [3/3] Reloading settings from backend...
✅ Settings reloaded, AI will use new language: ru
```

---

### 3️⃣ Гость (не авторизован)

```typescript
1. SettingsContext: settings = DEFAULT_SETTINGS
2. Cookie остаётся неизменным (default: "pl")
3. ✅ Гость видит default язык
```

---

## 📁 Изменённые Файлы

### 1. `contexts/SettingsContext.tsx` ✅

**Добавлена синхронизация backend → cookie:**

```typescript
// После загрузки settings из backend
if (currentCookieLang !== data.language) {
  console.log(`🔄 Updating cookie to match backend: ${data.language}`);
  
  // Обновить cookie
  document.cookie = `app_language=${data.language}; path=/; ...`;
  
  // Перезагрузить страницу
  window.location.reload();
}
```

**Результат:** Cookie ВСЕГДА совпадает с backend

---

### 2. `components/LanguageSwitcher.tsx` ✅ (уже правильно)

**Правильный поток:**

```typescript
const handleLanguageChange = async (lang: Language) => {
  // [1] Сохранить в backend (источник истины)
  await updateSettings({ language: lang });
  
  // [2] Перезагрузить из backend (гарантия синхронизации)
  await loadSettings();
  
  // [3] Обновить UI (reload страницы)
  setLanguage(lang);
};
```

**Результат:** AI, уведомления, UI используют ОДИН язык

---

### 3. `docs/LANGUAGE_ARCHITECTURE.md` ✅ (новый файл)

Полная документация архитектуры языка:
- Золотое правило: Backend = источник истины
- Диаграммы потоков
- Контрольный список
- Примеры кода
- Частые проблемы и решения

---

## ✅ Проверка

### Сценарий 1: Авторизованный пользователь с другим языком в backend

```bash
# 1. Установить cookie на "pl"
document.cookie = "app_language=pl; path=/"

# 2. В БД язык пользователя = "ru"
# 3. Перезагрузить страницу

# Ожидаемый результат:
# ✅ SettingsContext обнаруживает несовпадение
# ✅ Cookie обновляется на "ru"
# ✅ Страница перезагружается
# ✅ UI показывает русский язык
```

---

### Сценарий 2: Пользователь меняет язык в UI

```bash
# 1. Кликнуть "RU" в LanguageSwitcher
# 2. Проверить консоль:
#    ✅ [1/3] Saving language to backend: ru
#    ✅ [2/3] Language saved to DB: ru
#    ✅ [3/3] Reloading settings from backend...
# 3. Страница перезагружается
# 4. ✅ UI показывает русский язык
```

---

### Сценарий 3: Проверка AI

```bash
# 1. Сменить язык на RU
# 2. Открыть чат с AI
# 3. AI отвечает на русском
# 4. ✅ Язык AI совпадает с UI
```

---

## 🎓 Принципы

```
1. Backend First
   Backend всегда решает, какой язык использовать

2. Cookie as Cache
   Cookie используется ТОЛЬКО для SSR/middleware

3. Auto Sync
   При каждой загрузке: backend.language === cookie
   Если нет → обновить cookie + reload

4. Single Source of Truth
   SettingsContext загружает язык из backend

5. Reload on Change
   При смене языка страница перезагружается
```

---

## 📊 Результат

```
✅ Backend = ЕДИНСТВЕННЫЙ источник истины
✅ Cookie синхронизируется автоматически
✅ UI, AI, уведомления используют ОДИН язык
✅ Смена языка всегда проходит через backend
✅ Нет рассинхронизации между cookie и backend
```

**Статус:** ✅ Production Ready

---

## 🚀 Следующий Шаг

Исправить JWT: добавить `sub = user.id` в токен

См. `docs/JWT_FIX_SUB.md` (будет создан отдельно)
